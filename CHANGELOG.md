# Changelog

## v1.13.11

- 美化桌面 Widget 横向留白：今日看板 Widget 内容区左右 padding 从 6dp 提高到 14dp，上下 padding 从 6dp 提高到 10dp，避免标题、卡片和 Widget 圆角外壳贴边
- 美化倒数日 Widget 横向留白：运行布局和添加页预览外层左右 padding 从 4dp 提高到 12dp，上下 padding 从 4dp 提高到 8dp
- 倒数日卡片内部左右 padding 提高到 14dp，并放大竖线与文字区之间的呼吸感，让倒数时间、颜色竖线和事项标题不再挤在边缘
- 数据库保持 `25`，本轮不新增 schema 或用户数据格式迁移
- 版本号升级到 `1.13.11` / `versionCode 259`
- 验证：`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.11 / 259`

## v1.13.10

- 提升调试包版本号，确保 Android 在已安装 `1.13.9` 或同版本本地构建时把新 APK 识别为可升级安装包
- 本轮版本元数据沿用 `1.13.9` 工作线的 Widget follow-up 和日历 Pager 回归防护改动；不额外引入数据库结构、提醒逻辑、规划台逻辑或用户数据格式变更
- 数据库保持 `25`
- 版本号升级到 `1.13.10` / `versionCode 258`
- 验证：`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.10 / 258`

## v1.13.9

- 修正 Widget 待办和日程色条的一体化裁剪：色条 drawable 改为纯矩形，待办卡和日程事件行启用 `clipToOutline`，避免色条与卡片边缘出现拼接裂缝
- 统一 Widget 日程聚合卡与待办卡背景：浅色 / 深色 `widget_todo_item_background` 与软卡片底板保持同色、同圆角、同描边风格
- 深色模式新增逾期 badge 背景变体，提升 `已逾期` 标签可见度；loading 文案改为 `⏳ 加载中…` 以区别普通空状态
- 倒数日 Widget 和添加页预览的外层 padding 从 6dp 回退到 4dp，保持上一轮“收紧 padding”的目标
- 日历 Pager 回归防护：事件块长按拖拽期间禁用 `HorizontalPager` 用户翻页，降低事件横向改期与 Pager 翻页抢手势的风险；三日视图继续保持每次横向翻 1 天的浏览语义
- 数据库保持 `25`，本轮不新增 schema 或用户数据格式迁移
- 版本号升级到 `1.13.9` / `versionCode 257`
- 验证：`./gradlew.bat :app:compileDebugKotlin` 已通过；最终 `git diff --check`、`./gradlew.bat :app:assembleDebug`、APK 元数据和模拟器日历回归检查待执行

## v1.13.8

- Android 今日看板和倒数日 Widget 移除应用壁纸背景层，改用固定浅色暖白 / 深色灰黑圆角底板和极淡 scrim，添加页静态预览同步使用新背景
- 今日看板 Widget 待办卡去掉空心勾选圆圈，改为一体化圆角卡 + 左侧分组色条；逾期待办标签从 `!` 改为 `已逾期`
- 今日看板 Widget 增加自定义 `加载中…` RemoteViews，避免无日程或数据加载期间露出系统默认 loading；今明日程空状态继续显示明确文案
- 今日看板 / 倒数日 Widget 收紧外层、卡片、日程行和列表分隔留白，小尺寸桌面组件可显示更多内容
- 手机日历日视图 / 三日视图改由 `HorizontalPager` 驱动横向翻页，替代手动 `scrollable` 偏移；待办 LazyColumn 同类卡片补充 `contentType` 以降低大量列表滚动复用成本
- 数据库保持 `25`，本轮不新增 schema 或用户数据格式迁移
- 版本号升级到 `1.13.8` / `versionCode 256`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.8 / 256`；`Pixel_8 / emulator-5554` 安装后通过日历 deep link 启动，无 AndroidRuntime 崩溃

## v1.13.7

- 提升调试包版本号，确保 Android 在已安装 `1.13.6` 时把新 APK 识别为可升级安装包
- 本轮只调整版本元数据和当前状态文档；不主动引入新的数据库结构、提醒逻辑、规划台逻辑、Widget 逻辑或用户数据格式变更
- 版本号升级到 `1.13.7` / `versionCode 255`
- 验证：`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.7 / 255`

## v1.13.6

- 完成 6 项体验精进与 M1/M2/M4/M5 功能线：手机规划台 Outliner 支持同级长按拖拽排序、节点级撤销栈、待办页未来循环实例折叠、提醒页大字扁平化视觉、抽屉「更多」统一入口样式
- 新增待办「闹钟模式」：重要 DDL 可持续响铃/振动，5 分钟后降级为未处理提醒并按 2 分钟间隔短响重试 3 次；循环待办模板和后续实例继承该模式
- 新增每日摘要通知、全局搜索、数据健康检查与安全清理；每日摘要/日程进行中通知使用独立通知频道，数据健康清理仅删除已完成旧待办、空规划文档和过期 AI 报告
- 日程进行中通知按日程开始/结束独立恢复排程，避免提醒点已过但日程仍未开始或正在进行时漏掉常驻通知；调度失败关闭提醒时会同步清理常驻通知
- 数据库升级到 `25`，`todo_items` 和 `recurring_task_templates` 新增 `alarmMode`；备份/恢复、桌面同步和 Room schema 均保留新字段
- 版本号升级到 `1.13.6` / `versionCode 254`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.6 / 254`

## v1.13.5

- 提升调试包版本号，确保 Android 在已安装 `1.13.4` 时把新 APK 识别为可升级安装包
- 本轮只调整版本元数据和当前状态文档；不主动引入新的数据库结构、提醒逻辑、规划台逻辑或用户数据格式变更
- 版本号升级到 `1.13.5` / `versionCode 253`
- 验证：`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.5 / 253`

## v1.13.4

- 提升调试包版本号，确保 Android 在已安装 `1.13.3` 时把新 APK 识别为可升级安装包
- 本轮只调整版本元数据和当前状态文档；不主动引入新的数据库结构、提醒逻辑、规划台逻辑或用户数据格式变更
- 版本号升级到 `1.13.4` / `versionCode 252`
- 验证：`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.4 / 252`

## v1.13.3

- 提升调试包版本号，确保 Android 在已安装 `1.13.2` 时把新 APK 识别为可升级安装包
- 本轮只调整版本元数据和当前状态文档；不主动引入新的数据库结构、提醒逻辑、规划台逻辑或用户数据格式变更
- 版本号升级到 `1.13.3` / `versionCode 251`
- 验证：`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.3 / 251`

## v1.13.2

- 提升调试包版本号，确保 Android 在已安装 `1.13.1` 时把新 APK 识别为可升级安装包
- 本轮只调整版本元数据和当前状态文档；不额外引入新的数据库结构、提醒逻辑、规划台逻辑或用户数据格式变更
- 版本号升级到 `1.13.2` / `versionCode 250`
- 验证：`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.13.2 / 250`

## v1.12.19

- 收口 7 项运行问题修正：循环提醒调度失败不再拖垮启动，编辑 BottomSheet 禁止半屏收缩，倒数日 / 日程点击优先进入详情预览
- 待办编辑器新增「仅提醒，不在看板/日历显示」开关；该类待办仍正常调度提醒并保留在待办页，但会从每日看板、今日看板 Widget、桌面端看板和 AI 日报/周报统计中排除
- 循环待办模板保留「仅提醒」设置，后续自动展开的实例继承该设置；待办卡片和详情页显示「仅提醒」标记，便于在待办页管理
- 规划台 Outliner 新增备注节点：以 `// ` 或 `> ` 开头的节点会存为备注样式，不发布为待办/日程，也不参与父项完成状态计算；预览三点菜单支持「标记为备注 / 取消备注」
- 数据库升级到 `24`，新增 `todo_items.hiddenFromBoard`、`recurring_task_templates.hiddenFromBoard` 和 `planning_nodes.isNote`；JSON 备份/恢复与电脑同步 JSON 均保留这些字段，旧备份默认按 `false` 处理
- 今日看板和独立倒数日 Widget 进一步压缩卡片/列表留白，提高小尺寸桌面组件的可见条目数量
- 版本号升级到 `1.12.19` / `versionCode 246`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.19 / 246`

## v1.12.18

- 提升调试包版本号，确保 Android 在已安装 `1.12.17` 时把新 APK 识别为可升级安装包
- 当前工作区包含数据库 `24` 线上的规划台备注节点与仅提醒待办等未完成收口改动，调试包会随当前源码一起构建
- 版本号升级到 `1.12.18` / `versionCode 245`
- 验证：`./gradlew.bat :app:assembleDebug` 输出 `BUILD SUCCESSFUL`；`git diff --check` 通过；debug APK 元数据确认 `1.12.18 / 245`

## v1.12.17

- 提升调试包版本号，确保 Android 在已安装 `1.12.16` 时把新 APK 识别为可升级安装包
- 本轮只调整版本元数据，不主动引入新的数据库结构、提醒逻辑、规划台逻辑或用户数据格式变更
- 版本号升级到 `1.12.17` / `versionCode 244`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.17 / 244`

## v1.12.16

- 提升调试包版本号，确保 Android 在已安装 `1.12.15` 时把新 APK 识别为可升级安装包
- 本轮不改变数据库结构、提醒逻辑、规划台逻辑或用户数据格式，只重建更高版本调试包
- 版本号升级到 `1.12.16` / `versionCode 243`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.16 / 243`

## v1.12.15

- 日历顶部把月份标题和操作按钮合并到同一行，标题仍可点击选日期，窄屏下操作区可横向滚动
- 待办分组筛选把新建分组入口前置到 `全部` 右侧的紧凑 `+`，避免分组过多时需要滚到末尾才能新建
- 规划台恢复更显眼的文档按钮和图片识别入口，图片识别仅在存在 vision AI 源时可用
- 规划台 Outliner 增加草稿 / 发布分离：新建、分享、拍照、语音和图片识别先进入草稿节点，不再立即生成正式待办 / 日程；用户可单条发布或批量发布当前文档草稿
- 修复草稿节点编辑后的发布意图保留问题：编辑草稿不会再把同步标记误清空，发布后仍会按节点内容生成正式待办 / 日程
- 电脑端 Web 规划台同步支持草稿状态、单条发布和批量发布，草稿行用虚线样式和 `草稿` 元信息区分
- 更新手机端规划台教程、电脑端规划台帮助和内置 Wiki，明确“回车只是草稿，发布才入库”的新工作流
- 数据库升级到 `23`，`planning_nodes` 新增 `isDraft` 字段；备份 / 恢复保留草稿状态，旧备份默认按正式节点恢复
- 版本号升级到 `1.12.15` / `versionCode 242`
- 验证：`node --check app/src/main/assets/desktop-web/app.js`、`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.15 / 242`

## v1.12.14

- 提升调试包版本号，确保 Android 在已安装 `1.12.13` 时把新 APK 识别为可升级安装包
- 本轮不改变数据库结构、提醒逻辑、规划台逻辑或用户数据格式，只重建更高版本调试包
- 版本号升级到 `1.12.14` / `versionCode 241`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.14 / 241`

## v1.12.13

- 补齐手机端规划台 Outliner 备忘录级键盘行为：空输入行 Backspace 会回到上一节点，节点行首 Backspace 会与上一个同级节点合并，节点中间 Enter 会断行为新同级节点
- 手机端规划台输入行改为无完整边框的轻量 BasicTextField，保留 placeholder 和聚焦底色，使底部输入行/子任务输入行更像大纲自然延续而不是表单框
- 补齐电脑端 Web Outliner 同类键盘行为：空行 Backspace 删除并聚焦上一节点，行首 Backspace 合并同级节点，中间 Enter 断行，上下方向键可跨节点/根输入行移动焦点
- 版本号升级到 `1.12.13` / `versionCode 240`
- 验证：`node --check app/src/main/assets/desktop-web/app.js`、`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.13 / 240`

## v1.12.12

- 提升调试包版本号，确保 Android 在已安装 `1.12.11` 时把新 APK 识别为可升级安装包
- 本轮不改变数据库结构、提醒逻辑、规划台逻辑或用户数据格式，只重建更高版本调试包
- 版本号升级到 `1.12.12` / `versionCode 239`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.12 / 239`

## v1.12.11

- 修复手机端规划台 Outliner 已有节点编辑体验：编辑节点时按 Enter 会在当前节点下方打开并聚焦同级输入行，输入后按回车按原位置插入新同级节点，不再只是提交文本后停住
- 修复规划台已有节点编辑提交的防重入问题：Enter、输入法完成键和失焦提交只会处理一次，避免一次编辑触发重复保存或多余自动备份
- 修复规划台父子节点菜单语义：有子任务的父节点在预览 `⋯` 菜单里显示为“有子任务时保持结构标题”，不再给出实际不会生效的“同步为待办/日程”操作
- 修复规划台无子节点展开箭头的语义提示：展开子任务输入区时图标和无障碍文案保持一致，避免明明是展开入口却提示“折叠子任务”
- 版本号升级到 `1.12.11` / `versionCode 238`
- 验证：`node --check app/src/main/assets/desktop-web/app.js`、`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 输出 `PaykiTodo-1.12.11-debug.apk`

## v1.12.10

- 手机端规划台 Outliner 编辑体验改为更接近备忘录：大纲节点默认以轻量文本行展示，底部和展开的子任务区都有活跃输入行，输入后按回车即可创建节点，不再必须点“添加”按钮
- 规划台 Outliner 新增编辑 / 预览切换：编辑模式隐藏每行右侧操作按钮，预览模式通过每行 `⋯` 进行时间、地点、同步状态和删除配置，并可对已同步的叶子节点打开现有待办 / 日程完整编辑器
- 修正规划节点父子同步语义：有子项的节点会自动降级为结构标题并删除它原先关联的正式待办 / 日程，叶子节点才同步为正式事项；删除或移走最后一个子项后，普通父节点会恢复为叶子同步项
- 规划台自然日程解析支持 `时间, 事件名, 地点` 的裸地点写法，例如 `15:00-17:00, 写论文, 图书馆3楼` 和 `15:00-17:00 写论文 图书馆3楼`
- 规划台主菜单精简为新建、重命名、使用说明、归档、删除；Markdown 导出 / 导入移动到 Markdown 兼容模式工具栏
- 规划台新手教程重写为 3 页，覆盖回车创建节点、时间地点写法、子任务、完成同步、预览配置和 Markdown 兼容区
- 版本号升级到 `1.12.10` / `versionCode 237`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.10 / 237`

## v1.12.9

- 修复电脑端规划台 Outliner 拖拽排序的浏览器兼容风险：拖动节点时不再依赖 `dragover` 阶段读取 `dataTransfer`，改为记录当前拖拽节点状态，避免部分浏览器无法高亮落点或无法放置
- 电脑端 Outliner 现在同时保留上移 / 下移按钮和同层级拖拽排序；跨层级拖拽仍会被拦截，避免误改父子结构
- 版本号升级到 `1.12.9` / `versionCode 236`
- 验证：`node --check app/src/main/assets/desktop-web/app.js`、`git diff --check`、`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.9 / 236`

## v1.12.8

- 修复规划台 Outliner 与正式待办 / 日程的删除、取消同步漏洞：从待办页、日历页、提醒页或电脑端取消 / 删除由规划台节点创建的正式事项后，规划台节点会解除正式事项关联，不再在下次启动修复时被重新创建
- 修复规划台日程结束联动 DDL 待办的残留风险：删除或取消日程主关联项时，会同时处理该节点的结束 DDL 联动待办，避免留下半截提醒事项
- 数据库版本保持 `22`；本轮只修同步行为，不新增 Room schema
- 版本号升级到 `1.12.8` / `versionCode 235`

## v1.12.7

- 修复旧 Markdown 规划文档迁移到 Outliner 后的结构标题污染问题：`# 今日计划`、`# 收集箱` 等标题会保留为大纲结构节点，但默认不会同步生成正式待办 / 日程
- 规划台节点新增同步开关：普通事项继续“写下即同步”为正式待办 / 日程，结构节点可在手机端和电脑端显示为“结构标题”，电脑端可切换是否同步为正式事项
- 备份 / 恢复和桌面同步 API 均保留规划节点的同步语义，避免恢复后再次把结构标题补建成无 DDL 待办
- 数据库升级到 `22`，新增 `planning_nodes.syncEnabled`；`21 -> 22` 迁移会清理结构标题节点在 1.12.6 期间误生成的自动待办关联
- 版本号升级到 `1.12.7` / `versionCode 234`

## v1.12.6

- 修复电脑端规划台 Outliner 时间显示错误：节点 DDL / 开始 / 结束输入框和摘要现在优先使用后端返回的日期时间字符串，避免显示毫秒时间戳
- 修复电脑端规划台切换文档后的大纲错位风险：文档下拉切换时会重新加载对应文档的 Outliner 节点，不再保留上一个文档的大纲内容
- 版本号升级到 `1.12.6` / `versionCode 233`
- 验证：`node --check app/src/main/assets/desktop-web/app.js`、`git diff --check`、`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.6 / 233`

## v1.12.5

- 修复快速捕获在 Android 13+ 未开启通知权限时的误导提示：分享、拍照、语音和规划台后台捕获会提示“请稍后进规划台查看”，不再承诺一定收到通知
- 修复规划台 Markdown 兼容区的重复创建风险：按钮文案从“识别”改为“捕获”，执行前会确认当前文本会作为新事项写入大纲并同步创建待办 / 日程
- 版本号升级到 `1.12.5` / `versionCode 232`
- 验证：`node --check app/src/main/assets/desktop-web/app.js`、`git diff --check`、`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.5 / 232`

## v1.12.4

- 系统分享入口改为真正后台化：分享文字或图片到“添加到 PaykiTodo”后会立即返回，识别结果直接写入规划台节点并通过通知打开对应规划文档
- 删除规划文档会同步删除该文档大纲节点关联的待办、日程和日程结束 DDL 待办，并清理对应提醒痕迹
- 规划台节点的日程与结束 DDL 待办完成状态双向同步，手机端和电脑端完成操作都会处理所有受影响提醒
- 应用启动时会修复旧 Markdown 迁移后缺少关联待办 / 日程的规划节点，避免旧数据只显示在大纲里但没有正式提醒事项
- 版本号升级到 `1.12.4` / `versionCode 231`

## v1.12.3

- 规划台 Outliner 补齐手机端大纲行编辑：文本框支持 Enter / 输入法“下一项”创建同级事项、Tab 缩进、Shift+Tab 反缩进、空文本 Backspace 删除当前节点
- 规划台 Outliner 新增长按行菜单：可直接设置 DDL / 开始结束时间、设置地点或删除节点；时间输入支持 `16:30`、`明天 16:30`、`5.28 14:30` 等自然写法
- 规划台设置新增“显示编辑提示行”和“日程结束同步 DDL 待办”；日程节点可按设置额外创建以日程结束时间为 DDL 的联动待办，默认保持关闭
- 规划台节点数据升级到数据库 21：`planning_nodes` 新增 `linkedEndTodoId`，备份 / 恢复、删除、完成同步和提醒重排都会同时处理主关联事项和日程结束联动待办
- 电脑端规划台 Outliner 增加紧凑的上移 / 下移按钮，复用 `/api/planning/nodes/reorder` 更新同级节点顺序，不再只有后端排序接口
- 修复 Outliner 插入同级节点时可能出现相同 `sortOrder` 导致顺序不稳定的问题：指定位置插入会顺延后续同级节点
- 版本号升级到 `1.12.3` / `versionCode 230`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`node --check app/src/main/assets/desktop-web/app.js`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过

## v1.12.2

- 修复规划台 Outliner 完成父节点时的提醒同步漏洞：手机端和电脑端现在都会处理所有受影响子节点关联的待办 / 日程，而不是只清理当前节点的一个提醒
- 修复分享 / 拍照 / 语音捕获直写节点后的假提醒状态：如果系统拒绝排提醒，会把对应事项的 `reminderEnabled` 写回关闭，避免界面显示有提醒但实际没有闹钟
- 规划台 Outliner 当前已具备手机树形编辑入口、桌面 `/api/planning/nodes` 接口、桌面节点编辑 UI、捕获结果直写节点和 Markdown 导入 / 导出兼容入口
- 注意：手机端 Outliner 的 Enter / Tab / Shift+Tab / Backspace 键盘编辑、长按设置时间 / 地点菜单，以及桌面端真实拖拽 / 上下移动排序仍需继续补齐；本版本不应视为整个 Outliner 目标完成
- 版本号升级到 `1.12.2` / `versionCode 229`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`node --check app/src/main/assets/desktop-web/app.js`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过

## v1.12.1

- 新增规划台 Outliner 数据基础：数据库升级到 20，新增 `planning_nodes` 表，用于保存规划文档里的树形节点、父子关系、完成状态、时间、地点和关联待办 / 日程
- 新增规划台节点迁移：升级时会把旧 Planning Desk Markdown 中的标题、任务行、子任务行迁移为节点记录，并尽量保留原有导入映射关联
- 新增规划台节点备份 / 恢复：JSON 备份包含 `planningNodes`，恢复时会过滤无效文档、无效关联项和循环父节点，避免脏数据破坏树结构
- 新增规划台节点仓库能力：Repository / ViewModel 已具备节点创建、更新、完成切换、删除子树、排序、Markdown 导出和从 Markdown 替换导入的基础 API
- 修复 1.12.1 半成品接线导致的编译失败：`MainActivity`、`DashboardScreen`、`DashboardBody` 的规划节点回调已对齐，项目恢复可编译和可打包
- 注意：手机端 Outliner 树形编辑器、桌面端 `/api/planning/nodes` API / Web UI、捕获结果直接写入节点仍未完成，本版本不能视为完整 Outliner 功能闭环
- 版本号升级到 `1.12.1` / `versionCode 228`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:testDebugUnitTest`、`node --check app/src/main/assets/desktop-web/app.js`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.1 / 228`

## v1.12.0

- 新增系统分享捕获入口：从其他应用分享文字或图片到“添加到 PaykiTodo”后，会进入捕获识别预览并复用规划台导入管线写入待办 / 日程
- 新增长按桌面图标快捷方式：支持“拍照添加”和“语音添加”，拍照会通过 FileProvider 调起系统相机，语音会请求麦克风权限并显示实时转写
- 新增后台捕获识别通知：拍照、语音和规划台“识别”会先返回前台，完成后通过“PaykiTodo 捕获识别”通知进入候选预览
- 规划台“识别”改为后台处理，同时保留当前规划文档的原始内容，不会因为后台预览回写而裁剪首尾空行或重复追加整篇文档
- 数据库版本保持 `19`，阶段一没有引入 `planning_nodes` 表；Outliner 数据重构仍是后续阶段
- 版本号升级到 `1.12.0` / `versionCode 227`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`node --check app/src/main/assets/desktop-web/app.js`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.12.0 / 227`

## v1.11.4

- 修复 1.11.3 全屏签到页签退后按钮禁用的问题：同一个开启打卡追踪的日程现在可以多次签到 / 签退，签退后按钮显示“再次签到”而不是把后续签到入口锁死
- 修复全屏签到页绕过 ViewModel 后不触发自动备份的问题：签到和签退成功后会按当前自动备份设置导出最新快照
- 修复从全屏签到页返回后看板日程行、日历日程详情打卡区域状态可能不刷新的问题：主界面恢复时会重新读取该日程的打卡记录
- 版本号升级到 `1.11.4` / `versionCode 226`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.11.4 / 226`

## v1.11.3

- 日程打卡签到/签退改为独立全屏界面 `CheckInActivity`：显示日程名称、实时 HH:MM:SS 时钟、大圆形状态按钮（蓝色"签到"→ 绿色"已签到"→ 红色"已签退"），签退前弹确认对话框
- 移除每日看板的"快速签到"按钮和 BottomSheet，签到入口统一为日程行的"去签到"按钮和日程详情的"去签到/查看签到"按钮
- 日历日程详情的打卡区域不再内嵌签到/签退按钮，改为跳转签到界面
- 清理 ViewModel 中不再使用的 `quickCheckInEvent`、`adjustCalendarEventEndTime`、`checkInCalendarEvent`、`checkOutCalendarEvent` 方法
- 清理 AppSettingsStore 和备份中的 `lastQuickCheckInTitle`、`lastQuickCheckInLocation`、`lastQuickCheckInMinutes` 字段
- 版本号升级到 `1.11.3` / `versionCode 225`

## v1.11.2

- 每日看板的公告、倒数日、今日待办、今日日程、明日日程卡片新增展开 / 折叠控制，折叠状态会本地保存并进入备份，重启后仍保持用户选择
- 抽屉导航收口为每日看板、待办、日历、规划台、设置五个一级入口；AI 报告和历史记录移动到“更多 / 归档与历史”折叠区，深链入口仍然保留
- 待办多分组筛选新增交集 / 并集切换：多选分组时默认保持交集，空态可以一键切到并集查看属于任一分组的待办
- 规划台识别预览新增批量设置区，可对当前选中的候选统一加入倒数日、统一为日程创建联动待办、统一启用打卡，并可批量指定分组
- 规划台图片识别返回后会把 Markdown 追加到当前文档并自动进入识别预览；预览顶部显示可放大的原图缩略图，用户仍可返回编辑器修改源文本
- 规划台新增“今日”文档入口，今日文档会记录 `documentDateEpochDay`，无日期的时间段行会按文档日期解析；桌面同步规划台也传递该文档日期字段
- 每日看板新增“快速签到”：可快速创建从当前时间开始的打卡日程并立即签到，签退后显示投入结果并可按实际签退时间调整日程结束时间
- 日程打卡新增闲置自动签退兜底，默认阈值 2 小时；应用启动 / 恢复和小组件刷新时会把超过阈值的活跃签到按日程结束时间自动签退并发低优先级通知
- 桌面同步新增“桌面同步期间保持网络唤醒”开关，默认开启；同步服务运行时会按设置持有 WiFi 锁和 partial wake lock，并在前台通知中说明保活状态
- 数据库升级到 `19`，`planning_notes` 增加 `documentDateEpochDay`；备份 / 恢复同步保留新增的看板折叠、快速签到、桌面保活和自动签退设置
- 版本号升级到 `1.11.2` / `versionCode 224`
- 验证：`node --check app/src/main/assets/desktop-web/app.js`、`./gradlew.bat :app:compileDebugKotlin`、`git diff --check`、`./gradlew.bat :app:assembleDebug` 通过；debug APK 元数据确认 `1.11.2 / 224`

## v1.11.1

- 每日看板欢迎语默认折叠，首次使用提示移动到今日待办 / 今日日程之后，并压缩成更短的新手提示卡，避免首屏被引导内容占满
- 待办页底部操作层级调整：单条待办 `+` 保持主按钮，批量待办改为较弱的小型图标 FAB
- 待办批量导入默认打开空输入框，不再预填可能过期的示例；占位提示和“填入示例”改用相对未来日期，避免一打开就是错误态
- 规划台编辑器增加自然日程、DDL 和 Markdown 子任务的占位提示，空白文档也能直接知道怎么写
- 待办分组筛选说明缩短为交集筛选和长按管理；日历时区标签从原始 `GMT+0` 改为 `本地时间（UTC±偏移）`
- 版本号升级到 `1.11.1` / `versionCode 223`
- 验证：`./gradlew.bat :app:compileDebugKotlin`、`./gradlew.bat :app:assembleDebug` 通过

## v1.11.0

- 删除番茄钟式专注模式：抽屉入口、全屏专注页面、专注小组件、待办长按“开始专注”、设置里的专注配置、电脑端看板专注统计和 AI 日报专注字段都已移除
- 数据库迁移进入 `17 → 18`：升级时删除旧 `focus_sessions` 表，并把旧默认“专注”分组并入“例行”，避免已删除功能继续出现在分组 UI 中
- 备份恢复不再导出或恢复专注记录 / 专注设置；旧备份中残留的 `focusSessions` 字段会被静默忽略
- 日程打卡追踪完整落地：日程编辑器可开启 `打卡追踪`，日程详情、全屏提醒、无障碍提醒、每日看板和电脑端 Web 都能查看或触发签到 / 签退
- 完成已开启打卡的日程时可自动签退，并按设置显示计划时长、实际投入、签到次数和投入率统计；AI 日报同步新增 `今日日程投入`
- 待办多分组通过 `todo_group_tags` 落地：手机端和电脑端都可给待办选择多个分组，筛选多个分组时按交集显示
- Room schema 导出已开启，`app/schemas/com.example.todoalarm.data.AppDatabase/18.json` 记录数据库 18 结构；`ui-tooling-preview` 改为 debug-only 依赖，避免 release 继续携带预览工具依赖
- AI 日报 / 周报设置新增“报告保留时长”下拉项（30 天 / 90 天 / 365 天 / 永久），生成新报告后会按设置清理过期归档；该设置会进入备份但不导出 API Key
- 应用启动初始化改用带 `SupervisorJob` 和非致命异常记录的 application scope，避免裸 `CoroutineScope(Dispatchers.IO)` 静默吞掉初始化失败
- 抽屉导航把“我的任务”收口为单行“待办”，删除展开分组列表和独立“分组管理”入口；待办页顶部新增横向分组筛选 chip，支持点选筛选、长按编辑分组和新建分组
- 备份导出 / 导入新增 `todoGroupTags`，旧备份会按原 `groupId` 回填多分组关联，避免升级或恢复后丢失原有分组颜色和筛选结果
- 电脑同步 `/api/todos` 和 `/api/snapshot` 输出并接受 `groupIds`，电脑端 Web 待办编辑器、筛选 chip、卡片、预览和每日看板行都会保留并展示全部分组；桌面端日程预览也可查看打卡记录并执行签到 / 签退
- 规划台快捷栏精简为 `子任务` 和 `公告`，日程 / DDL / 提醒 / 分组继续通过自然文本和标签输入，避免快捷栏变回按钮堆
- 规划台新增 `从图片识别日程`：可选择课表 / 日程截图，调用标记为支持图片识别的 AI 源，把识别出的 Markdown 追加到当前规划文档，再由现有“识别”预览流程导入
- AI 源配置新增 `此服务支持图片识别` 能力位；图片识别按 OpenAI-compatible vision 消息格式调用，失败时按 Provider 顺序回退
- AI 日报 / 周报、课表模板、桌面规划台等热路径改用范围查询或单条查询，电脑同步业务路由改成 suspend handler，避免在业务层到处 `runBlocking`
- Android `今日看板` 小组件去掉顶部三横线 / 标题 / 日期栏，独立 `倒数日` 小组件改成可滚动每日看板风格列表；两个小组件都由 Provider 自己按分钟刷新，`updatePeriodMillis=0`
- release 构建启用 R8 和资源收缩，三张看板背景图改为 WebP；当前 `PaykiTodo-1.11.0-release.apk` 为 `4.83 MB`
- 版本号升级到 `1.11.0` / `versionCode 222`
- 验证：`./gradlew.bat :app:assembleRelease`、`./gradlew.bat :app:assembleDebug`、release APK 元数据检查、`apksigner verify`、`git check-ignore` 和 `Pixel_8 / emulator-5554` release 启动 / 主入口 smoke test 通过

## v1.10.3

- 规划台轻量日程默认不再同步创建待办；只有在预览中显式勾选“同步创建以日程结束时间为 DDL 的待办任务”时才会额外生成待办
- 修复联动待办被自动写入固定备注的问题：即使手动选择同步待办，也只保留用户自己写的备注
- 本地规则和 AI 识别都补强地点解析：`@图书馆3楼`、`"@主楼B1-412"`、`地点：主楼B1-412` 会进入日程地点字段，不再污染标题，也不会重复补 `@`
- AI 识别提示和结果清洗收紧：默认 `createLinkedTodo=false`，并阻止模型从 `【课程】习思想` 或 `入党表格填写` 这类标题里擅自拆出分组
- 电脑端日程编辑器补齐与手机端一致的 8 个颜色预设圆点，同时保留自定义颜色输入
- Android `今日看板` 小组件去掉顶部三横线、标题和日期栏，列表内容直接占满小组件主体，避免跨日后顶部日期与列表状态不一致
- Android `倒数日` 小组件改为可滚动列表，卡片使用看板软底色、动态颜色竖线和更大的倒计时 / 标题文字，长标题与时间信息可多行显示
- Android `今日看板` 和 `倒数日` 小组件都改为由 AlarmManager 分钟级自调度刷新，不再依赖系统最低 30 分钟的 `updatePeriodMillis`
- 版本号升级到 `1.10.3` / `versionCode 221`

## v1.10.2

- 每日看板的倒数日卡片改为按真实剩余时长显示：1 天以上显示 `Nd` + 剩余小时分钟，不足 1 天显示 `Nh` + 剩余分钟，不足 1 小时显示 `Nm`，不再显示秒或总小时数
- 倒数日日程行去掉多余的 `日程` 前缀，只保留日程发生时间；已过点的倒数目标会从手机看板、电脑端看板和倒数日小组件中隐藏
- 手机每日看板、电脑端每日看板和 Android 今日看板小组件的“今日日程”数量改为统计尚未结束的日程，今天日程都结束时显示完成提示且计数为 0
- Android 独立 `倒数日` 小组件去掉标题、日期和项数头部，只保留倒计时、颜色竖线和目标标题；点击待办 / 日程行会直接进入对应详情
- `倒数日` 小组件增加每分钟刷新调度，让分钟级倒计时更接近实际剩余时间
- 每日看板移除专注统计和自由专注入口，新增独立 `专注` 抽屉页面；绑定待办专注保留在 `我的任务`，自由专注保留在 `专注` 页面
- 新增独立 `专注` 小组件，显示今日专注统计并可直接启动自由专注
- Android 小组件补齐选择器名称、说明、建议尺寸和静态预览，避免添加小组件时只看到无名或空白预览
- 版本号升级到 `1.10.2` / `versionCode 220`

## v1.10.1

- 修复电脑端规划台 AI 识别预览字段不完整的问题：日程候选现在可以在右侧预览中编辑地点、全天、倒数日和重复规则，导入后会写入手机端日程的对应字段，不再把地点塞进备注
- AI 识别 JSON 约束新增 `location` 和 `recurrence` 字段，明确 `@主楼B1-412` 是用户原文地点，不会额外补 `@`
- 今日看板小组件不再嵌入倒数日分区，倒数日改为只通过独立 `倒数日` 小组件展示
- 独立 `倒数日` 小组件调整为更接近 App 每日看板倒数日卡片：待办保留完成圆圈，日程不显示圆圈；倒计时从 `D-N` 改为 `Nd` + 小时/分钟/秒，日程显示完整开始-结束时间
- App 每日看板和电脑端每日看板的倒数日行同步去掉 `D-N` 文案和重复类型尾巴，日程元信息显示完整发生时间
- 版本号升级到 `1.10.1` / `versionCode 219`

## v1.10.0

- 新增倒数日模式：待办编辑器和日程编辑器都可以打开“倒数日”，待办按 DDL、日程按开始时间计算剩余天数
- 每日看板新增倒数日卡片，展示最近的关键目标，并保留点击进入待办 / 日程编辑的入口
- 电脑端每日看板同步展示倒数日，电脑端待办 / 日程编辑表单也支持保存倒数日开关
- Android 新增独立 `倒数日` 小组件，显示最近 3 个倒数目标
- 数据库升级到 `17`，为 `todo_items` 和 `recurring_task_templates` 增加 `countdownEnabled` 字段，并补充倒数日查询索引；备份和电脑同步 JSON 会保留该字段
- 版本号升级到 `1.10.0` / `versionCode 218`

## v1.9.23

- 修复小组件日程地点重复显示 `@` 的问题：地点内容现在按用户保存的原文展示，`@主楼B1-412` 会显示为 `@主楼B1-412`，不会再被小组件额外补成 `@@主楼B1-412`
- 修复电脑同步开启后设置页可能只显示“服务未运行”而不给连接地址的问题：打开开关时会立即确保局域网服务启动，状态查询也会在已启用但服务尚未建立时主动拉起服务，设置页改为提示“正在启动”而不是阻断连接地址入口
- 调整日程批量导入和 Wiki 说明：地点字段不再写成 `@地点` 这种疑似强制前缀的格式，明确 `@` 只是地点字符串的一部分，想显示才手写
- 清理小组件专注卡已删除后遗留的夜间 drawable 资源，修复 release 构建 lintVital 对缺少默认资源的阻断
- 版本号升级到 `1.9.23` / `versionCode 217`

## v1.9.22

- 修复 Android 今日看板小组件头部日期已更新、列表内容仍停留在前一天的问题：系统更新、日期变化、手动改时间 / 时区、应用替换后都会同步刷新 RemoteViews 列表数据，并把当天日期写入列表适配器缓存键，降低桌面启动器复用旧列表的概率
- 提升小组件浅色模式可读性：浅色卡片改为更实的暖白底板，内层提示块改为暖米色，文字、分区标题和蓝色日期色值加深，避免白底上文字发灰发白
- 版本号升级到 `1.9.22` / `versionCode 216`

## v1.9.21

- 修正手机日历页顶部栏标题仍显示英文 `Schedule` 的问题，统一为中文 `日历`，避免中文界面里混入英文入口名
- 版本号升级到 `1.9.21` / `versionCode 215`

## v1.9.20

- 手机日历页改为按当前视图可见日期范围订阅活动日程，并在三日 / 单日 / 月视图 / 列表视图切换时更新查询窗口，避免进入日历页就观察全部未完成日程
- 日程通知或小组件深链打开某个远期日程时，会先扩展手机日历查询窗口并定位到目标日期，保留远期日程直达能力
- 无 DDL 活动待办的今日分类逻辑抽成共用函数，手机每日看板和我的任务继续走同一套“每天归入今日待办”规则
- 补充 `TodoItemSectionsTest`，确认无 DDL 待办跨天仍在今日待办且不会被归入计划中
- 版本号升级到 `1.9.20` / `versionCode 214`

## v1.9.19

- 电脑同步服务改为有上限的连接处理线程池，避免局域网异常连接或浏览器重复请求导致线程无限增长
- 电脑同步 HTTP 请求改为按字节读取请求头和请求体，修复中文规划台长文本保存 / 导入时可能因 UTF-8 字节长度和字符长度不一致被截断的问题，并对异常或过大的请求返回明确错误
- `AI 报告` 归档新增关键词搜索、日报 / 周报类型下拉和近 7 / 30 / 90 天时间范围筛选，筛选条件下推到 Room 查询，不再只在内存里过滤已加载列表
- 日历时间轴的 1461 天日期范围改为轻量日期窗口按需计算日期和索引，减少进入日历时的大列表与索引表分配
- 数据库版本升级到 `16`，新增桌面待办分页排序索引和 AI 报告类型 / 生成时间复合索引，降低大数据量下分页、搜索和归档筛选的扫描压力
- 版本号升级到 `1.9.19` / `versionCode 213`

## v1.9.18

- 电脑端待办管理列表新增分页和搜索：`/api/todos` 支持 `offset`、`limit` 和 `q`，浏览器端可搜索标题 / 备注 / 地点，并显示“已加载 / 总匹配”与“加载更多”
- 电脑端待办分页保持看板语义：已错过、今日 / 无 DDL、计划中、历史记录按业务优先级排序，避免无 DDL 活动待办被未来或历史数据挤到后面
- Android 今日看板小组件继续移除专注内容：运行时删除 Focus 行类型和专注卡分支，添加页静态预览也不再显示“今日已专注 / 自由专注”
- 小组件今日待办不再固定截断为 6 条，改由整体 RemoteViews 行数上限控制，较大尺寸的小组件可以露出更多待办
- 版本号升级到 `1.9.18` / `versionCode 212`

## v1.9.17

- 电脑端 Web 新增 `/api/todos` 和 `/api/events?start=...&end=...` 窄接口，待办管理和日程时间轴不再共用一个完整 `/api/snapshot`
- 电脑端首次连接仍只读取轻量每日看板；用户点击“加载完整待办列表”时只拉待办，切到日程时间轴时只拉当前可见日期范围内的活动日程
- 电脑端待办 / 日程保存、删除、完成、取消和规划台导入后的刷新改为按当前页面补齐数据，避免每次操作都重新读取全部待办和日程
- 无 DDL 活动待办在电脑端完整待办列表中继续归入 `今日待办`，与手机每日看板 / 我的任务语义保持一致
- 版本号升级到 `1.9.17` / `versionCode 211`

## v1.9.16

- 规划文档新增持久化公告提示字段 `hasAnnouncementHint`，保存正文、备份恢复和电脑端规划台同步时都会按 Markdown 正文重新计算，避免公告增删后看板读取旧状态
- 数据库版本升级到 `15`，`MIGRATION_14_15` 会为旧规划文档回填公告提示字段并创建 `archived + hasAnnouncementHint` 查询索引
- 每日看板、Android 小组件和电脑端轻量快照的公告候选查询改为走布尔字段和索引，不再对完整 Markdown 正文做 `LIKE` 扫描
- 补充 `PlanningNote` 默认公告提示单测，确认公告正文会自动标记，普通规划正文不会误标记
- 版本号升级到 `1.9.16` / `versionCode 210`

## v1.9.15

- 普通每日看板 / 我的任务状态不再携带完整规划台文档列表；规划台完整文档改为只有进入 `规划台` 页面时才单独订阅，避免长规划文档拖慢普通页面重组
- 每日看板公告改用规划文档公告提示窄查询：只读取疑似包含 `#公告`、`[!公告]`、`[!announcement]` 或 `公告:` 的未归档规划文档，再交给现有公告解析器做日期和正文过滤
- Android 今日看板小组件和电脑端 `/api/snapshot` 同步改用公告提示查询生成公告，不再为了看板首屏读取全部规划台文档
- 补充公告提示识别单测，确认常见公告入口能命中、普通规划正文不会误命中
- 版本号升级到 `1.9.15` / `versionCode 209`

## v1.9.14

- 手机端主状态继续瘦身：每日看板 / 我的任务不再订阅历史待办、完整日历模板和提醒链路诊断日志，历史记录、日历模板和设置诊断改为进入对应页面时才单独订阅
- 主界面待办数据改为按活动待办 Flow 查询，保留无 DDL 活动待办每天归入 `今日待办` 的语义，同时避免普通看板被历史记录列表拖慢
- 日历页打开时再订阅完整活动日程和课程模板；每日看板只订阅今明两天相关日程，减少远期日程对首页重组的影响
- 提醒链路诊断日志只在设置页订阅，不再随每日看板 / 我的任务状态更新
- 规划台本地规则识别移到后台线程执行；AI 失败回退本地规则时也不会占用 Compose 主线程解析长文档
- `AI 报告` 归档页改为按筛选和数量分页订阅，默认只加载最近 30 条，通知深链可按 ID 单独打开未在当前页内的旧报告
- 版本号升级到 `1.9.14` / `versionCode 208`

## v1.9.13

- 电脑端每日看板首屏改用轻量快照：连接后先请求 `/api/snapshot?scope=board`，只加载今日看板、今明日程、公告、活动规划文档和今日专注统计；进入日程时间轴或点击“加载完整待办 / 日程数据”时再读取完整数据
- 手机端主状态不再把完整专注记录和完整 AI 报告列表合并进普通每日看板 / 我的任务状态；今日专注统计改用 Room 聚合 Flow，AI 报告只在进入 `AI 报告` 页面时单独订阅
- 日历视图复用顶层事件日期索引，月视图、列表视图和可见全天日程不再各自重复按日期重建事件分组，降低日程数量变多后的重组开销
- 补充无 DDL 待办跨天单测，确保无 DDL 活动待办不是只在创建当天显示，而是每天都归入 `今日待办`
- 电脑端待办管理区文案改为“首屏优先加载每日看板、完整管理视图按需加载”，避免轻量数据模式下误导用户
- 版本号升级到 `1.9.13` / `versionCode 207`

## v1.9.12

- 修复 Android 今日看板小组件漏显示无 DDL 待办的问题：小组件的 board-range Room 查询现在会显式取出无 DDL 活动待办，和手机每日看板 / 桌面每日看板的“无 DDL 归入今日待办”语义一致
- 数据库版本升级到 `14`，新增 `todo_items` 看板待办、看板日程、活动提醒、分组 DDL、循环系列 DDL 复合索引，减少每日看板、小组件刷新、提醒扫描和分组/循环查询的大表扫描压力
- 电脑端 `/api/snapshot` 去掉一次重复的分组读取，复用同次快照已加载的分组列表生成待办 / 日程显示所需的分组映射
- 规划台普通项目符号识别为无 DDL 待办时，预览候选会提示“普通项目符号已识别为无 DDL 待办”，降低把普通笔记误认为正式待办的理解成本
- 设置 -> AI 调用配置在编辑、启用/停用、排序、删除 AI 源后会尽量立即保存有效配置；如果仍有未保存或必填项缺失，会在页面内直接提示，降低填完 Base URL / API Key 后忘记保存的风险
- 设置 -> 电脑同步补充 5 分钟无授权连接自动关闭的说明，明确只打开网页但未输入正确密钥不算已连接
- 版本号升级到 `1.9.12` / `versionCode 206`

## v1.9.11

- 无 DDL 活动待办统一归入“今日待办”：手机每日看板、Android 小组件、电脑端每日看板和电脑端待办管理列表都会把它当作今天要处理的事项，不再把它塞到计划中 / 单独漂浮区
- 规划台本地解析器新增普通项目符号任务识别：`- 想办的事`、`* 整理材料`、`• 发消息` 会识别为无 DDL 待办，仍然支持写时间段时识别为日程
- 电脑同步开启后，如果 5 分钟内没有任何带正确访问密钥的电脑端 API 请求，会自动关闭电脑同步并停止前台服务，避免长期空跑
- 电脑同步连接地址按更可能可用的 Wi-Fi 地址优先排序，并在设置页说明 `10.*` / `172.*` 等地址通常来自 VPN、热点或虚拟网卡，不是 localhost
- 设置 -> AI 调用配置的 AI 源列表改成摘要卡 + 开关 + 更多菜单，避免 Base URL、模型、排序和删除操作挤在同一行
- 版本号升级到 `1.9.11` / `versionCode 205`

## v1.9.10

- Android 今日看板小组件点击语义改为入口级跳转：点击待办区域进入应用内“我的任务”，点击日程 / 聚合日程区域进入“日历”，不再直接打开具体待办或日程详情
- Android 今日看板小组件移除“今日已专注 / 专注一下”卡片，让桌面小组件只保留公告、问候、今日待办和今明日程摘要
- 待办编辑器新增“提醒方式”选择，可在全屏提醒和通知栏提醒之间切换；桌面 Web 待办编辑器同步补齐该字段
- 全屏提醒页和无障碍提醒浮层拆分“延后提醒”和“DDL 推迟”：延后提醒只调整下一次提醒，不再自动修改 DDL；DDL 推迟新增独立输入，支持分钟增量、当天时刻和完整日期时间，并要求新 DDL 晚于原 DDL
- 版本号升级到 `1.9.10` / `versionCode 204`

## v1.9.9

- 电脑端 Web 第一个入口从纯待办时间轴升级为每日看板视图：顶部显示今日日期和专注统计，下方展示今日待办、今日日程、明日日程，并保留原待办时间轴作为管理区
- 电脑端 `/api/snapshot` 新增 `todayBoard` 数据，复用手机端每日看板的日程过滤逻辑，已经结束的今日日程不再作为当前事项展示，正在进行的日程会用金色描边和“进行中”标识突出
- 修复电脑端规划台 AI 识别后导入 0 条的问题：后端导入接口现在能直接接收 AI 返回的候选 ID，不再要求候选必须匹配本地规则解析出的 `line-*` ID
- 收紧规划台 AI 分组逻辑：AI 只有在原文显式写出 `#group`、`分组：`、`项目：` 或 `课程：` 时才保留 `groupName`，避免 `16:05-18:00 入党表格填写` 被误拆出“入党”分组
- 版本号升级到 `1.9.9` / `versionCode 203`

## v1.9.8

- AI 日报 / 周报改为写入独立 `AI 报告` 归档入口，不再创建或更新规划台 `AI 日报` / `AI 周报` 文档
- 新增 `ai_reports` Room 表、旧规划台报告一次性迁移、备份 / 恢复中的 `aiReports` 数组，升级后历史报告会保留并从规划台文档列表移除
- 抽屉新增 `AI 报告`：支持全部 / 日报 / 周报过滤、卡片预览、详情查看、长按或详情删除前确认，空状态会引导到设置里的 AI 日报 / 周报配置
- AI 报告生成通知改为携带报告 ID，点击通知直达对应报告详情；设置页“立即生成一次日报”也写入归档而不是规划台
- `了解 AI 日报` 从偏左的底部 Sheet 改为居中对话框，并把报告位置文案改为“抽屉 → AI 报告”
- 版本号升级到 `1.9.8` / `versionCode 202`

## v1.9.7

- 规划台快捷栏新增「公告」入口：展开快捷操作后可一键在新行插入 `#公告 `，继续填写日期范围和正文即可同步到每日看板、电脑端 Web 横幅和 Android 小组件
- 设置 -> AI 调用配置 -> AI 日报 / 周报 新增「了解 AI 日报」说明 Sheet，分卡片解释日报/周报汇总什么、启用前提、开启步骤、报告查看路径和常见问题
- 协作与维护文档补充：完成过的 goal prompt 需要单独归档提交；Android Emulator 预验证需要记录设备、APK 和实际检查过的界面，避免后续混淆调试来源
- 归档 `1.9.0` 专注模式和 `1.9.1` AI 日报 / 周报的 goal prompt 文件
- 版本号升级到 `1.9.7` / `versionCode 201`

## v1.9.6

- Android 今日看板小组件继续按 App 内每日看板收口：顶部栏去掉“轻触打开”提示并压缩为更接近日看板 TopBar 的圆形菜单 + 标题 + 日期层级
- 小组件待办卡补充分组标签和备注摘要，DDL 文案改为 `⏰ DDL HH:mm`，卡片去掉重描边并缩小内部留白，让它更像 App 内待办卡而不是系统列表行
- 小组件聚合日程卡压缩日期块和事件行留白，正在进行的日程会使用金色边框 / 淡金底，普通日程保持透明行 + 竖色条
- 版本号升级到 `1.9.6` / `versionCode 200`

## v1.9.5

- AI 日报 / 周报调度在 Android 12+ 未授予「精确闹钟」权限时不再直接依赖 `setExactAndAllowWhileIdle`，会安全降级为系统允许的 idle 调度，避免定时保存后静默失败或启动时抛权限异常
- 设置 -> AI 调用配置 -> AI 日报 / 周报 补充说明：若未开启「精确闹钟」权限，日报 / 周报可能被系统延后生成
- 版本号升级到 `1.9.5` / `versionCode 199`

## v1.9.4

- Android 今日看板小组件继续重做为每日看板视觉：添加页预览改用静态看板式 previewLayout，不再只显示空列表式初始布局
- 小组件默认尺寸提升为 4x5 更接近竖向每日看板，外层背景、顶部栏、标题日期层级和列表间距继续向 App 内看板靠拢
- 小组件待办卡改为更接近 App 内待办卡：保留分组色条，增加复选框视觉和 `DDL HH:mm` 小标签，减少旧版时间列的列表感
- 小组件日程卡取消普通日程行的小底板，改为看板卡内透明行 + 竖色条，贴近日内看板的呈现方式
- 版本号升级到 `1.9.4` / `versionCode 198`

## v1.9.3

- Android 今日看板小组件继续按 App 内每日看板重做视觉：外层留白加大、顶部栏更接近日看板标题区，列表行间距改为卡片堆叠节奏
- 小组件默认建议高度提升为更接近竖向看板卡片的尺寸，减少桌面添加后只像一条扁平系统列表的概率
- 小组件问候卡、专注卡、待办卡、空状态和聚合日程卡提高浅色 / 深色底板不透明度，并统一 24-28dp 圆角层级
- 小组件待办色条加宽，日程行增加上下留白、文字与色条间距和轻量行底板，使待办 / 日程更接近每日看板中的卡片块
- 版本号升级到 `1.9.3` / `versionCode 197`

## v1.9.2

- Android 今日看板小组件默认建议尺寸从扁平列表改为更接近每日看板的方形 / 竖向卡片区域，减少桌面添加后只露出系统列表行的概率
- 小组件顶部栏进一步压缩高度，保留每日看板标题、日期和圆形菜单按钮，但让问候卡、专注卡、待办和日程卡成为视觉主体
- 小组件问候卡、专注卡、待办卡、空状态和聚合日程卡改成更接近 App 内每日看板的实心圆角卡底板，弱化玻璃边框和系统 ListView 割裂感
- 小组件待办色条、日程色条、正文间距和日程行上下留白继续对齐 App 内每日看板，避免文字贴边和色条拥挤
- 版本号升级到 `1.9.2` / `versionCode 196`

## v1.9.1

- 新增 AI 日报 / 周报：日报默认每天 22:00、周报默认每周日 22:00，可在设置 -> AI 调用配置中启用、调整生成时间或立即生成一次日报
- 日报会汇总今天完成待办、错过待办、今日日程、明日日程、明天 DDL 和今日专注分钟；周报会汇总本周完成 / 错过待办、本周日程、下周 DDL 和本周专注分钟
- 报告优先复用规划台 AI 多源轮询；AI 未启用、配置失败或调用失败时会用本地模板兜底，仍写入规划台「AI 日报」/「AI 周报」文档
- 报告文档按最新内容在最上方追加，打开 AI 报告文档时规划台会显示紫色提示条，提醒这是自动生成的复盘记录
- 报告生成后发送低优先级通知，点击通知可直接打开对应规划台报告文档；开机、时区或时间变化后会重新调度日报 / 周报
- Android 今日看板小组件继续向 App 内每日看板对齐：公告回到问候卡之前，顶部标题区、问候卡、专注卡、待办卡、空状态和聚合日程卡进一步压缩留白并提高卡片底板可读性
- 版本号升级到 `1.9.1` / `versionCode 195`

## v1.9.0.1

- Android 今日看板小组件继续向 App 内每日看板收口：根容器缩小顶部工具栏占用，保留背景图和遮罩，但让卡片流成为主体
- 小组件新增“今日已专注”卡片，显示今日专注分钟、总专注次数和完成次数，与 App 内每日看板的专注统计顺序保持一致
- 小组件问候卡、分区标题、待办卡、空状态卡、公告横幅和聚合日程卡统一加大圆角卡片感、加入轻量层级、调整字号和留白，减少“系统列表控件”的割裂感
- 小组件深色 / 浅色遮罩和卡片底板不透明度重新调节，提升桌面背景下卡片文字可读性
- 版本号升级到 `1.9.0.1` / `versionCode 194`

## v1.9.0

- 新增专注模式：待办卡片长按菜单可选择“开始专注 · X 分钟”，进入全屏番茄钟倒计时
- 新增自由专注入口：每日看板的“今日已专注”统计卡可直接启动不绑定待办的自由专注
- 专注倒计时支持圆环进度、暂停/继续、提前完成确认、放弃确认、归零震动、延续 N 分钟和 1.5 秒完成反馈页
- 新增 `focus_sessions` Room 表和 `MIGRATION_11_12`，记录专注主题、绑定待办、计划/实际时长、开始/结束时间、完成或放弃、延续次数
- 每日看板新增“今日已专注”统计卡，显示今日完成专注分钟、总专注次数和完成次数
- 设置 -> 专注模式新增默认专注时长、单次延续时长、屏幕常亮和专注期间压制提醒偏好；压制提醒当前仅记录偏好，不拦截系统通知
- JSON 备份 / 恢复包含专注记录和专注设置
- 版本号升级到 `1.9.0` / `versionCode 193`

## v1.8.9

- Android 今日看板小组件外层改为直接叠加每日看板背景图和浅/深色遮罩，视觉上更接近 App 内每日看板而不是普通系统列表
- 小组件顶部从右侧应用图标改为左侧圆形菜单按钮 + “每日看板”标题结构，对齐 App 内每日看板顶部栏的阅读方式
- 小组件公告改为橙色圆角横幅，待办、空状态、问候卡和聚合日程卡提高底板不透明度并放大关键标题，减少“灰色列表块”的廉价感
- 聚合日程卡和待办卡增大色条宽度与正文间距，使日程/待办信息更接近日内看板卡片层级
- 版本号升级到 `1.8.9` / `versionCode 192`

## v1.8.8

- Android 今日看板小组件外层改为固定“每日看板”标题区，显示当天日期和轻触打开提示，视觉入口更接近 App 内每日看板而不是普通列表控件
- 小组件头部右侧改用透明应用图标缩略图，替代临时文字圆标，减少桌面小组件的拼装感
- 小组件卡片底板改为更接近每日看板的浅色 / 深色圆角渐变与半透明卡片层级，保留 RemoteViews 兼容性，避免整张背景图在桌面上出现方角裁切风险
- 问候卡、空状态、公告、待办卡和聚合日程卡进一步压缩留白；待办和日程文字关闭额外字体内边距，提升桌面小组件小尺寸下的可读面积
- 聚合日程卡内的日程色条改为跟随行高拉伸，标题、时间和地点字号略收紧，使日程区域更像每日看板中的时间安排块
- 版本号升级到 `1.8.8` / `versionCode 191`

## v1.8.7

- Android 今日看板小组件进一步贴近日常 App 内每日看板：顶部新增问候卡，不再只显示普通列表标题
- 小组件背景改为浅色 / 深色渐变看板底板，降低“系统列表组件”的割裂感
- 小组件待办卡新增左侧分组色条，颜色来自任务分组，视觉上更接近每日看板任务卡
- 小组件日程区改为单张聚合看板卡：左侧显示今天日期，右侧集中展示今日日程、完成提示、明天标题和明日日程摘要，不再把每条日程拆成独立大日期卡
- 日程看板卡内的日程行继续保留单条点击跳转到对应日程详情
- 版本号升级到 `1.8.7` / `versionCode 190`

## v1.8.6

- Android 今日看板小组件改为更接近 App 每日看板的卡片式视觉：分区标题独立显示，空状态使用深色圆角卡片，不再是一排排线框小条
- 小组件日程行改为看板式日程卡：左侧显示日期 / 星期 / 日号，中间使用日程颜色竖线，右侧显示标题、时间和地点
- 小组件继续保留待办、日程、公告的行级跳转；点击分区标题或空状态仍回到每日看板
- 小组件深色配色改为更接近每日看板的深色实心卡片，并移除行卡片的重描边
- 版本号升级到 `1.8.6` / `versionCode 189`

## v1.8.5

- 设置 -> AI 调用配置的单个 AI 源编辑弹窗新增“获取模型”：只填写 Base URL 和 API Key 后，App 会尝试 OpenAI-compatible `/models` 接口拉取可用模型列表
- 模型拉取兼容服务根地址、`/v1`、完整 `/chat/completions` 和完整 `/models` 地址；服务根地址会优先尝试 `/v1/models`，再尝试 `/models`
- 如果用户把 Base URL 填成完整 `/models` 地址，后续“测试连接”和真实识别会自动转换到同级 `/chat/completions`，避免模型获取成功但测活失败
- 获取到模型后会显示紧凑下拉选择行并默认选中第一个模型；模型名输入框仍保留，拉取失败或服务不支持 `/models` 时仍可手动填写
- 模型拉取失败会区分 API Key 无效 / 无权限、404 endpoint 不支持、非 JSON 网页响应和不兼容 JSON 格式，避免只看到不可操作的网络错误
- 版本号升级到 `1.8.5` / `versionCode 188`

## v1.8.4

- 电脑端 Web 公告横幅改为长度感知滚动：60 字以内静止显示，长公告或多条公告自动横向滚动，鼠标悬停暂停阅读
- 电脑端 Web 控制台新增跟随系统的深色模式，待办时间轴、日程时间轴、规划台、弹窗、统计卡片、侧栏卡片和公告横幅均切换到深色变量配色
- Android 今日看板小组件支持行级跳转：点击待办行进入对应待办编辑，点击日程行跳到日历并打开详情，点击公告行进入来源规划文档，标题和空状态行回到默认每日看板
- Android 今日看板小组件在完全没有待办、日程和公告时只显示一条“今天没有安排，去 App 创建吧”，不再展示多个空分区
- 启动页隐藏逻辑从固定 600ms 改为数据就绪即隐藏，并保留 800ms 最大兜底，减少冷启动等待和数据闪烁风险
- 版本号升级到 `1.8.4` / `versionCode 187`

## v1.8.3

- 清理旧设置公告字段：公告彻底改由规划台文档驱动，`AppSettings`、设置备份导出和旧设置入口不再保存单条公告；旧备份里的 announcement 字段会被兼容忽略
- 公告识别更灵活：`- [ ] #公告 ...`、`> #公告 ...`、行内 `#公告 ...` 也能识别，尾部 `#imported` / `#group` / 普通 hashtag 不再污染公告文本
- 规划台 Markdown 预览中公告行显示橙色边框、喇叭图标和“全局公告”标签，点击公告行可回到对应原文行编辑
- 公告排序改为近期日期公告优先，长期公告排在最后；电脑端 Web 控制台 `/api/snapshot` 也会返回公告并在顶部显示公告横幅
- Android 今日看板小组件补齐深色模式资源，文字和背景色随系统夜间模式切换；小组件刷新改用看板范围窄查询并移除 `onReceive` 双重刷新
- 补充 AI 识别验证文档，记录 1.8.1 触发收紧和 1.8.2 Base URL endpoint fallback 的手动测试步骤
- 版本号升级到 `1.8.3` / `versionCode 186`

## v1.8.2

- Android 桌面小组件从“今日待办”升级为“今日看板”：同一列表内展示规划台公告、今日待办、今日日程和明日日程摘要，保留点击打开 App 的行为
- 小组件行高改为自适应，增加最小拖动尺寸配置，不再硬限制最多 5 条内容，桌面拖大后可自然露出更多看板条目
- 公告不再从设置页单独维护；规划台任意未归档文档可写多条 `#公告` 或 `> [!公告]` 行，支持日期范围和长期公告，并同步显示在每日看板与小组件
- 修复部分 OpenAI-compatible AI 源无法连接的问题：Base URL 填服务根地址时优先尝试 `/v1/chat/completions`，也兼容已填写 `/v1` 或完整 `/chat/completions` 的情况
- AI 测试连接超时放宽到 10 秒连接 / 20 秒读取；服务返回 HTML 或其他非 JSON 内容时会提示检查 Base URL，而不是暴露 `<!doctype` JSON 转换错误
- 版本号升级到 `1.8.2` / `versionCode 185`

## v1.8.1

- 规划台 AI 识别触发收紧：手机端仍只在点“识别”时调用识别链路，电脑端导入不再在没有预览结果时隐式触发 `/api/planning/parse`，而是提示先点击“识别”
- 规划台自动保存状态从进度条改为普通“自动保存中”文字，避免用户把换行后的保存反馈误认为 AI 正在调用
- 设置 -> AI 调用配置的单个 AI 源编辑弹窗新增“测试连接”，会用当前 Base URL / API Key / 模型发起 10 秒内测活请求，并显示成功字数或 HTTP/超时错误
- 每日看板新增公告横幅：设置页可填写公告文本与起止日期，生效期内在看板顶部显示橙色喇叭横幅，长文本支持横向滚动，过期自动隐藏
- 新增 Android 桌面小组件第一版：系统桌面可添加 PaykiTodo 今日待办小组件，显示当天最多 5 条活动待办，点击小组件或条目打开 App
- 启动页展示时间从 1.6 秒缩短到 0.6 秒
- 新增 `docs/current/AI_RECOGNITION_VERIFICATION.md`，记录手机端和电脑端 AI 识别调用链、四种状态的 UI 表现和手动验证步骤
- 版本号升级到 `1.8.1` / `versionCode 184`

## v1.8.0

- 规划台导入现在除了给原文追加 `#imported`，还会为每一条导入行创建稳定映射，记录它对应的正式待办或日程，规划台不再只是一次性导入器
- 新增 `planning_line_mappings` 数据表与数据库 `11` 号迁移；映射定位不再只依赖旧行号，而是结合去噪后的文本指纹和模糊匹配，尽量容忍插行和轻度改写
- 手机端规划台 Markdown 预览现在直接显示映射状态：`已导入 / 已完成 / 已取消 / 映射丢失 / 已手动修改`；其中已完成项可通过“同步完成状态到原文”写回为 `- [x]`
- `刷新已导入项` 现在只会刷新未完成且未取消的映射项；已完成/已取消项会跳过，原文找不到会标记为映射丢失，正式事项被手动改动会标记为冲突
- 新增规划台“批量顺延”能力，可按分区或文档尾部批量平移未完成事项，并同步改写原始 Markdown 中的时间文本
- 最近一次导入、刷新或顺延都可以撤销；撤销导入会删除正式事项并去掉 `#imported`，撤销顺延会同时恢复事项时间和原文时间
- 规划台冲突现在支持双向处理：既可以“以文档为准覆盖事项”，也可以“以事项为准更新原文”
- 电脑端 Web 规划台同步补齐映射状态预览、刷新、批量顺延、撤销和冲突处理入口，继续操作手机端同一份本地数据
- 规划文档映射记录现在会随 JSON 备份 / 恢复一起保存
- 版本号升级到 `1.8.0` / `versionCode 183`

## v1.7.25

- 规划台“识别”现在会在启用 AI 调用配置且存在完整 AI 源时优先调用 OpenAI-compatible 接口，内置 prompt 会把随手写的计划拆成受限 JSON 候选
- AI 返回结果会转换为现有识别预览卡片，仍可在导入前修改标题、分组、备注、DDL、日程起止时间和提醒；AI 不会直接写入待办或日程
- 多 AI 源按设置页顺序轮询；401/403/429/5xx、超时、DNS 等可重试失败会尝试下一个源，AI 失败或配置不完整时自动回到本地规则识别
- 电脑端 Web 规划台的 `/api/planning/parse` 现已复用同一条 AI 优先 / 本地规则兜底识别链路，预览摘要也会显示当前是 AI 结果还是本地回退
- 手机端和电脑端规划台编辑框不再内置示例 placeholder，默认直接给空白编辑区；示例保留在教程和帮助页，避免在 AI 优先场景里先把用户带回旧语法
- 设置页和规划台教程文案更新为“AI 已可用但可选增强”，强调 API Key 本地保存、备份不导出、预览确认后再导入
- 新增 AI JSON 解析单元测试，覆盖 todo/event 候选、代码块 JSON、默认提醒和已导入行跳过
- 版本号升级到 `1.7.25` / `versionCode 182`

## v1.7.24

- 新手引导卡改用 `surfaceVariant` + `onSurface` 配色，深色模式下不再出现低对比文字；设置 -> 关于 -> 使用说明新增“重新显示新手引导”，可把引导卡恢复到下次打开看板时显示
- 规划台本地解析器补齐中文自然 DDL 表达：日期上下文下的“晚上/上午/下午/中午/早上”、`5点前` / `16:30之前` / `明天下午3点前`，以及无 checkbox 的 `截止` / `deadline` / `ddl` 行；检测到“每天/每周”等循环词时会在预览提示导入后到编辑器设置循环
- 设置 -> AI 调用配置改为多 AI 源列表，可新增、编辑、删除和启用/禁用多个 OpenAI-compatible 源；配置按列表顺序作为后续 AI 识别轮询优先级，旧单源配置会自动迁移为第一项，备份仍不导出真实 API Key
- 新增 `PlanningAiCaller.callWithFallback`，预留后续 AI 接入时按源顺序重试 401/403/429/5xx、超时和网络解析失败；400 和用户取消不会盲目重试
- 待办编辑器默认折叠高级字段：新建任务只显示标题、DDL 和分组，备注、提醒、循环、响铃/震动收进“更多选项”；编辑已有复杂任务时会自动展开
- 版本号升级到 `1.7.24` / `versionCode 181`

## v1.7.23

- 每日看板新增首次使用引导卡，告诉新用户如何创建待办、打开规划台和检查提醒权限；点"知道了"后默认关闭
- 看板"明天暂无日程"改为可点击跳转到规划台，引导用户安排明天
- 设置页电脑同步区域新增"复制"按钮，可一键复制连接地址和访问密钥
- 待办和日程编辑器的提醒输入 placeholder 改为具体例子"例：5（提前5分钟）或 5,15,16:30"
- 规划台使用说明新增第 7 页"其他导入方式"，串联批量待办和日程批量导入的入口
- 版本号升级到 `1.7.23` / `versionCode 180`

## v1.7.22

- 设置页新增 `AI 调用配置` 入口，可在手机端填写规划台后续 AI 识别需要的服务名称、Base URL、API Key 和模型名
- AI 配置只做本机保存，当前规划台仍默认使用本地规则识别；备份 JSON 会保存服务名称、Base URL 和模型名，但不会导出真实 API Key，导入备份也不会覆盖清空本机已有 Key
- 规划台快捷操作改为默认收起，只保留轻量“展开快捷操作”入口；需要插入任务、子任务、DDL、提醒、分组时再展开图标工具条，优先让出正文书写空间
- 版本号升级到 `1.7.22` / `versionCode 179`

## v1.7.21

- 规划台快捷栏改成更接近手机备忘录工具条的图标化小按钮，去掉外层大卡片和“快捷”文字，减少编辑区视觉负担
- 规划台顶部增加独立教程入口，使用说明改为多页新手教程，重点说明“自然写计划 -> 图标补结构 -> 识别 -> 预览确认 -> 导入”
- 规划台解析器补齐轻量裸 `ddl` 写法，例如 `任务M ddl 15:00` 会识别为今天 15:00 截止的待办，不再强制用户写 `#ddl`
- 增加规划台 AI 识别设计文档和 Provider 配置模板，明确后续可接入 DeepSeek / Qwen / OpenAI-compatible 接口，但 AI 只生成候选并必须经过预览确认
- 版本号升级到 `1.7.21` / `versionCode 178`

## v1.7.20

- 修正规划台快捷栏按钮点击无反馈/疑似无效的问题：快捷按钮不再使用空 `onClick` 的 `AssistChip`，改为单一路径的可点击 Surface
- 快捷栏每次执行后显示 `已执行：按钮名`，长按仍显示该按钮的用途说明
- 版本号升级到 `1.7.20` / `versionCode 177`

## v1.7.19

- 修复日程批量导入的中文输入法分隔问题：自定义语法正文会先归一化中文逗号、中文冒号和全角分隔符，再切分字段
- 现在 `13:40-14:40，标题，@地点` 这类中文逗号输入也能按“时间段 / 标题 / 地点”解析
- 版本号升级到 `1.7.19` / `versionCode 176`

## v1.7.18

- 放宽手机端日程批量导入语法：首条不写日期时默认今天，支持直接写 `13:40-14:40, 标题, @地点`
- 日程批量导入日期前缀支持 `今天`、`明天`、`后天`、`5.28`、`5/28`、`5月28日`、`2026-05-28`、`2026年5月28日`
- 日程批量导入帮助、示例和问号语法说明改为推荐“默认今天”的轻量写法
- 规划台顶部操作按钮会先清除输入焦点，并对预览/识别/文档列表给出即时反馈，避免点了按钮但看不出是否执行
- 版本号升级到 `1.7.18` / `versionCode 175`

## v1.7.17

- 修正规划台手机端顶部操作区过高的问题：操作区改为固定 56dp 的工具条，不再占用大块空白
- 操作区只保留预览 / 识别 / 文档列表 / 更多操作，文档标题与说明不再挤在同一行，避免窄屏下布局异常
- 自动保存状态保留为短文本提示，平时不占用主要操作空间
- 版本号升级到 `1.7.17` / `versionCode 174`

## v1.7.16

- 规划台手机端操作区从三行压缩为一行紧凑布局：标题 + 预览/识别按钮 + 溢出菜单，编辑区可视面积增加约 40%
- 次要操作（新建/重命名/使用说明/归档/删除）收入溢出菜单，移除手动保存按钮（依赖已有的 2 秒自动保存）
- 快捷栏从编辑器下方移到上方，减少被输入法遮挡的概率
- 暗色模式 onSurfaceVariant 对比度从 4.2:1 提升到 5.3:1，辅助文字在暗色背景下更清晰
- 电脑端 Web 控制台内容区增加 max-width: 1200px 约束，宽屏下不再过度拉伸
- 版本号升级到 `1.7.16` / `versionCode 173`

## v1.7.15

- 修正规划台解析优先级：一行同时包含自然时间段和显式 `#ddl` 时，按待办处理，避免用户明确写 DDL 却被识别为日程
- 日期上下文标题支持更自然的紧凑写法，例如 `# 周五计划`、`# 明天安排`、`# 5/28周末计划`；同时继续避免 `# 我的明天计划`、`# 后天的事` 这类描述标题被误判为日期
- 补充规划台解析器单元测试，覆盖显式 DDL 优先级和紧凑日期标题边界
- 版本号升级到 `1.7.15` / `versionCode 172`

## v1.7.14

- 规划台手机端新增 2 秒防抖自动保存，并在切换规划文档前先保存当前未保存内容，降低编辑后切换或退出导致正文丢失的风险
- 修复手机端快捷栏 AssistChip 双重点击监听，避免一次点击插入两次任务、标签或标题
- 修复规划台导入成功后 `#imported` 只更新界面、不立即持久化的问题，避免下次打开同一行重复导入
- 识别预览的导入按钮现在要求至少选中一条有效候选；新增“全选可导入项 / 全不选”，预览 Sheet 和文档列表改为按屏幕高度自适应
- 预览模式点击任务或文本可回到对应原文行；在文档中间按回车也会续写 Markdown 任务行；预览编辑 DDL / 起止时间补齐自然日期写法
- 电脑端 Web 规划台新增编辑器自动保存、切换文档前保存、`Ctrl+S` 保存、`Ctrl+Enter` 识别、导入前空选中拦截和预览全选控制
- 版本号升级到 `1.7.14` / `versionCode 171`

## v1.7.13

- 修正规划台自然日程标题污染：行内写前导日期时，标题不再包含日期和时间段，例如 `5.28 14:00-16:00 小组讨论` 会识别为标题 `小组讨论`
- 收紧日期标题上下文：普通标题会清空旧日期上下文，`# 我的明天计划` 不再被误判为明天，`# 5/28 周末计划` 这类日期加描述标题可以提供日期上下文
- 自然日程不再要求时间段必须在行首，`复习 14:00-16:00` 也会识别为日程；顶层日程也能作为缩进子任务的父级备注来源
- 补齐斜杠日期、常见全角分隔符、中文 AM/PM 和全角时间段符号的解析与测试覆盖
- 未实现的 `#today`、`#tomorrow`、`#important`、`#project` 不再被静默从标题中剥掉，并在 Wiki / 规划台设计文档中明确为暂不支持的语义标签
- 版本号升级到 `1.7.13` / `versionCode 170`

## v1.7.12

- 补齐语法说明覆盖面：手机端规划台说明、待办批量弹窗、日程批量弹窗、内置 Wiki、电脑端 Web 规划台说明和电脑端提醒输入占位示例同步到当前语法
- 明确待办批量添加的字段边界：英文逗号用于分隔 `DDL时间,任务名称,提醒时间`，字段内部连接日期和时间时建议使用空格或中文逗号
- 明确规划台 / 提醒输入支持 `明天 16:30`、`周五 16:30`、`5月28日，14:30` 等自然写法，电脑端预览编辑时间字段也使用同一套规则
- 更新内置说明中日程批量 `Remind=` 的示例，说明其支持编辑界面同款提醒语法，并提示批量字段内逗号的使用边界
- 版本号升级到 `1.7.12` / `versionCode 169`

## v1.7.11

- 对当前输入语法做一致性自检后，补齐两个明显不一致点：待办批量 DDL 和电脑端 Web 提醒输入
- 待办批量导入的 DDL 现在复用规划台日期解析，支持 `5.28`、`5月28日`、`明天`、`周五`、`2026年5月28日` 等自然写法；只写日期默认 `23:59`
- 待办 / 日程提醒输入进一步补齐 `明天 16:30`、`周五 16:30` 这类相对日期 / 星期日期写法
- 电脑端 Web 待办 / 日程提醒输入补齐 `2:30 pm`、`明天 16:30`、`周五 16:30`、`5.10 15:00`、`5月10日 14:30`、完整中文日期和中文逗号分隔
- 提醒解析补齐 `5.28,14:30` / `5月28日，14:30` 这种“日期和时间用逗号衔接”的自然写法；规划台电脑端预览编辑后的 DDL / 起止时间也复用同一套日期解析
- 更新输入帮助、Wiki、README 和 current 文档，明确“推荐自然写法”和当前仍保留的日程批量严格格式边界
- 版本号升级到 `1.7.11` / `versionCode 168`

## v1.7.10

- 规划台新建 / 默认文档改为空白正文，示例内容只作为输入框灰色 placeholder，不再真实写进文档
- 抽出通用提醒文本解析器，规划台 `#remind`、待办/日程提醒输入、批量导入提醒和自定义延后共用同一套解析规则
- 提醒输入新增对点号日期、中文日期、AM/PM 时刻等常见写法的支持，例如 `5.10 15:00`、`5月10日 14:30`、`2:30 pm`
- 规划台快捷栏修正为语义动作：“任务”只会把当前行变成一个 checkbox，“子任务”会新起一行并缩进
- `# 今日计划`、`# 今天`、`# 明天` 等标题现在明确作为文档分区和日期上下文使用，`# 今日计划` 也会真实影响下面未写日期的时间段解析
- 手机端规划文档目录新增单篇文档删除入口，电脑端 Web 规划台也新增当前文档删除按钮
- 规划台预览中的提醒输入改为支持混合提醒写法，非法时保留原输入、显示错误并阻止导入
- 版本号升级到 `1.7.10` / `versionCode 167`

## v1.7.9

- 将 commit message 规则写入 `AGENTS.md` 和会话工作流文档：提交信息应重点描述功能行为变化和 bug/debug 修复逻辑，而不是 push 状态或验证命令
- 新增 `docs/current/PLANNING_DESK_EXAMPLES.md`，系统说明规划台的详细用法、`# 收集箱` / `# 今日计划` / `# 明天` / `# 本周计划` 等一级标题的用途、常用语法和完整范例
- 扩充手机端规划台说明页，明确标题是用于文档分区和日期/主题上下文，不是必须语法或神秘标签
- 电脑端 Web 规划台新增“使用说明”按钮和内置说明弹窗，覆盖标题分区、常用写法、完整范例、识别与导入流程
- 版本号升级到 `1.7.9` / `versionCode 166`

## v1.7.8

- 在 `1.7.5` 数据库迁移修复后，重新恢复 `1.7.2` / `1.7.3` 之前未能实际使用的规划台 Markdown 渲染能力
- 规划台新增安全的“编辑 / 预览”切换：默认仍打开原文编辑，只有用户主动点“预览”才渲染 Markdown，避免旧文档在启动时阻断 App
- Markdown 预览支持标题层级、`- [ ]` / `- [x]` 任务 checkbox、子任务缩进、`#tag` 胶囊和 `#imported` 已导入状态胶囊
- 在预览里点击任务 checkbox 会回写原始 Markdown 行，但不会直接完成已经导入的正式待办，避免语义混乱
- 预览渲染失败时显示保护提示并允许回到“编辑原文”，不丢失原始内容
- 版本号升级到 `1.7.8` / `versionCode 165`

## v1.7.7

- 规划台顶部操作区新增“说明”入口，用户可以在规划台内直接打开使用说明，不需要跳转浏览器或设置页
- 新增规划台底部说明页，按真实工作流说明：先自由写计划、常用写法、识别预览、导入正式待办/日程、`#imported` 防重复导入
- 说明页提供可直接照抄的 Markdown / 自然文本示例，覆盖待办、子任务、DDL、提醒、分组和日程时间段
- 说明页明确当前限制：手机端仍是稳定原文编辑，富文本渲染、AI 拆解、拖拽排期暂未恢复或实现
- 版本号升级到 `1.7.7` / `versionCode 164`

## v1.7.6

- 确认 `1.7.5` 已解决启动闪退后，开始修正规划台手机端 UI 的可读性和使用意愿问题
- 规划台主界面从半透明叠背景的临时界面改为纯色卡片结构，顶部信息区、正文编辑区和快捷输入区层级更清楚
- 规划正文编辑区改为纯色大卡片，降低背景干扰，提升 Markdown / 自然文本输入时的可读性
- 快捷输入从两排占屏按钮改成单行横向滚动纯色工具条，减少视觉噪声
- 规划文档列表改为可滚动纯色列表，选中文档更明确，文档多时不再挤出屏幕
- 识别预览页改为更紧凑的纯色导入检查界面，导入统计、原文行和候选编辑卡片层级更清楚
- 版本号升级到 `1.7.6` / `versionCode 163`

## v1.7.5

- 修复 1.7.x 从 1.6.x 覆盖安装后仍可能打开即闪退的问题：`planning_notes` 的 Room 实体 schema 与 `8 -> 9` 数据库迁移 SQL 不一致，会在启动打开数据库时触发 Room 校验失败
- 数据库版本升级到 `10`，新增 `9 -> 10` 自愈迁移，会把 1.7.0-1.7.4 可能创建出的异常 `planning_notes` 表重建为 Room 期望结构
- 修正 `8 -> 9` 迁移，直接创建与当前 `PlanningNote` 实体一致的表结构，兼容从 1.6.x 直接升级到 1.7.5
- 保持 1.7.4 对不稳定手机端 Markdown 渲染的回退，不重新引入该启动风险
- 版本号升级到 `1.7.5` / `versionCode 162`

## v1.7.4

- 紧急止血：回退 1.7.2 / 1.7.3 引入的手机端规划台 Markdown 渲染实现，恢复 1.7.1 已验证稳定的规划台纯文本编辑与可编辑识别预览
- 保留规划台 Phase 2 的核心可用能力：文档搜索、识别预览内编辑、导入后自动追加 `#imported`、电脑端 Web 预览内编辑
- 暂停在手机端加载 Markdown 渲染组件，优先保证 App 能正常打开和使用
- AI 后续展望文档继续保留，但不接入任何 AI API
- 版本号升级到 `1.7.4` / `versionCode 161`

## v1.7.3

- 紧急修复 1.7.2 在手机端安装后打开即闪退的问题：规划台启动时不再自动渲染历史 Markdown 文档
- 规划台现在始终先进入 `编辑全文` 模式，Markdown `预览` 由用户主动点击触发，避免坏文档或渲染兼容问题阻断 App 启动
- Markdown 预览增加异常保护，解析或渲染失败时降级提示并允许回到编辑全文
- 移除规划台预览中的实验性 FlowRow 布局，改用更稳的 Column/Row 组合降低设备兼容风险
- 版本号升级到 `1.7.3` / `versionCode 160`

## v1.7.2

- 手机端规划台新增 Markdown 阅读渲染模式，默认不再直接裸露显示 `- [ ]` / `- [x]` 任务语法
- Markdown 阅读模式支持标题层级、任务 checkbox、缩进子任务、`#tag` 胶囊和 `#imported` 的“已导入”状态胶囊
- 阅读模式下点击任务 checkbox 会直接切换原始 Markdown 行的 `- [ ]` / `- [x]` 状态，但不直接完成正式待办
- 保留“编辑全文 / 预览”切换，底层仍保存原始 Markdown，确保识别、导入、电脑同步和备份逻辑不被破坏
- 将后续 AI 规划助手展望写入规划台设计文档：本地规则优先、OpenAI-compatible Provider、用户确认后再导入、不让 AI 直接改数据库
- 版本号升级到 `1.7.2` / `versionCode 159`

## v1.7.1

- 完成规划台 Phase 2 日常可用增强：手机端规划文档列表新增标题/正文搜索，便于在多文档中快速切换
- 手机端规划台识别预览从只读卡片升级为可编辑卡片，可在导入前修正标题、分组、备注、DDL、日程起止时间、提醒分钟和“同时创建待办”选项
- 规划台导入成功后会自动把原文对应行追加 `#imported`，并立即回写当前规划文档，减少重复导入风险
- 电脑端 Web 规划台同步支持预览内编辑和导入后回写 `#imported`，与手机端共用同一套导入校验与强提醒默认策略
- 规划台导入接口支持编辑后的候选数据，并继续阻止标题为空、过期 DDL、过期提醒、日程结束早于开始等非法条目
- 新增 `PlanningMarkdownParser.markImportedLines` 单元测试，覆盖导入标记追加和避免重复追加
- 版本号升级到 `1.7.1` / `versionCode 158`

## v1.7.0

- 新增手机端抽屉入口 `规划台`，支持像备忘录一样编辑 Markdown / 自然语言规划文档
- 新增多文档规划台数据表与 Room v9 迁移，支持新建、打开、重命名、归档、删除，并默认恢复最后一次打开的文档
- 新增本地 `PlanningMarkdownParser`，可识别 `- [ ]` 待办、已完成跳过、子任务备注、日期上下文、常见 DDL 格式、`#group` / `#remind` / `#schedule` 标签，以及 `10:00-12:30 作业1` 这类轻量日程写法
- 规划台识别结果先进入预览再导入；导入日程时默认可同时创建一条 DDL 等于日程结束时间的待办
- 规划台导入默认使用提前 5 分钟、全屏提醒、响铃 + 震动的强提醒策略
- 手机端规划台增加快捷输入栏：任务、子任务、缩进、减少缩进、DDL、日程、提醒、分组、今天、明天；回车会尽量续写 Markdown 任务行
- 规划文档纳入 JSON 备份 / 恢复，避免换机或导出时丢失前置规划内容
- 电脑端 Web 控制台新增 `规划台` Tab，支持同一份规划文档的查看、编辑、保存、识别预览和导入选中
- Wiki、README、当前状态文档同步新增规划台使用说明
- 版本号升级到 `1.7.0` / `versionCode 157`

## v1.6.83

- 设置页“提示音”恢复为二级面板，可在内置提醒音和系统通知提示音之间切换，避免只剩系统提示音入口
- Todo 编辑器补齐滚轴式“农历 DDL”入口，复用共享农历日期选择对话框，选择农历日期后保留原 DDL 时分
- 日历顶部改成月份标题独立一行、操作按钮独立一行，避免 `2026年5月` 继续被按钮挤成省略号
- 每日看板日程行增大蓝色竖线和文字的间距，增加上下留白，并收小时间行字号
- 版本号升级到 `1.6.83` / `versionCode 155`

## v1.6.82

- 预备修复设置页提示音二级面板、Todo 农历 DDL 入口、日历标题留宽和每日看板日程行距；随后在 `1.6.83` 中继续收口为滚轴农历 DDL 与标题拆行布局
## v1.6.81

- 电脑端已有日程卡片点击恢复为直接打开编辑弹窗；使用真实手机 snapshot 的 Node DOM 模拟验证了点击事件 `15` 会填充编辑器标题和 `editingEventId`，不会停在预览页
- 设置页电脑同步关闭时不再显示“连接地址”区块，减少旧局域网地址残留造成的误解
- 每日看板日程行继续收紧左侧色条间距，普通日程保持无外框，只有正在进行的日程显示金色边框
- 日历顶部改为左侧年月弹性卡片 + 右侧紧凑操作按钮，降低月份标题被按钮挤没的概率
- 版本号升级到 `1.6.81` / `versionCode 153`
## v1.6.80

- 更新内置 Wiki 的电脑同步说明：地址需要手动展开显示，关闭同步或服务未运行时不会显示地址，并补充当前电脑端预览 / 编辑能力
- 版本号升级到 `1.6.80` / `versionCode 152`
## v1.6.79

- 电脑端定时日程卡片补充按钮语义、键盘焦点和 aria 标签，使 Enter / Space 打开预览的路径真正可达
- 版本号升级到 `1.6.79` / `versionCode 151`
## v1.6.78

- 定时日程编辑也支持农历开始 / 农历结束选择；选择农历日期后保留原有时分，只替换对应日期并自动校正起止顺序
- 版本号升级到 `1.6.78` / `versionCode 150`
## v1.6.77

- 清理手机端日程编辑弹窗中“全天日程”的冗余说明文字，保留实际开关和农历开始 / 结束操作
- 版本号升级到 `1.6.77` / `versionCode 149`
## v1.6.76

- 手机端电脑同步开关 / 换密钥后强制刷新同步运行态，避免设置页继续使用旧状态显示残留访问地址
- 版本号升级到 `1.6.76` / `versionCode 148`
## v1.6.75

- 电脑端日程卡片点击改为先打开日程详情预览，再通过预览页进入编辑 / 删除，和手机端的简单预览交互保持一致
- 全天日程编辑增加农历开始 / 农历结束选择入口，可输入农历年月日并转换为对应公历日期
- 日历标题从硬裁切改为可换行省略，降低“2026年5月”这类标题被裁掉的概率
- 每日看板日程行继续收紧左侧色条间距，普通日程保持无外层边框，只有进行中日程保留金色高亮
- 版本号升级到 `1.6.75` / `versionCode 147`
## v1.6.74

- 电脑端待办 / 日程预览弹窗继续向手机底部 sheet 靠拢，统一宽度、头部按钮列和正文间距
- 版本号升级到 `1.6.74` / `versionCode 146`

## v1.6.73

- 日历时间轴事件卡片和全天日程条增加稳定 Compose key，减少横向滑动、增删改后事件节点被无谓重建的概率
- 版本号升级到 `1.6.73` / `versionCode 145`

## v1.6.72

- 电脑端日程 / 待办编辑弹窗继续收紧，改为更接近手机底部 sheet 的单列视觉布局
- 版本号升级到 `1.6.72` / `versionCode 144`

## v1.6.71

- 手机端设置页的电脑同步地址区进一步收紧，默认只显示启用 / 运行状态与手动展开入口，不再默认暴露局域网 URL
- 版本号升级到 `1.6.71` / `versionCode 143`

## v1.6.70

- 日历浏览器头部改成标题单独一行、操作按钮单独一行，避免“2026年5月”在窄屏下被按钮挤没
- 日历头部操作按钮改为第二行横向滚动，更符合窄屏手机的可用性
- 版本号升级到 `1.6.70` / `versionCode 142`

## v1.6.69

- 每日看板日程行继续保持普通日程无外层线框，只让正在进行的日程保留金色边框
- 收紧每日看板日程行左侧色条与内容块边缘的距离，并让进行中日程与普通日程的色条对齐更一致
- 版本号升级到 `1.6.69` / `versionCode 141`

## v1.6.68

- 清理手机端待办编辑器中的冗余说明文案，保留 DDL、提醒、历史任务和循环任务的实际操作与必要限制提示
- 清理设置页提醒声音策略中的长段解释文字，保留播放通道、内部音量、工作模式和临时提升音量等核心设置项
- 版本号升级到 `1.6.68` / `versionCode 140`

## v1.6.67

- 手机端设置页的电脑同步地址默认折叠：电脑同步服务运行但未主动查看连接信息时，不再直接显示 `http://...:18765` 地址
- 电脑同步二级页保留“显示连接地址 / 隐藏连接地址”操作，避免影响新电脑首次连接
- 版本号升级到 `1.6.67` / `versionCode 139`

## v1.6.66

- 电脑端已有日程点击编辑链路再次加固：日程卡片和全天日程入口在渲染后直接绑定点击编辑，不再只依赖时间轴外层事件委托
- 电脑端时间网格线、半小时线和当前时间线不再接收鼠标事件，降低它们覆盖日程卡片导致点击落空的风险
- 当前机器访问 `http://192.168.0.100:18765/` 超时，无法完成真实手机服务页面验证；本轮已完成源码链路检查与 `app.js` 语法验证
- 版本号升级到 `1.6.66` / `versionCode 138`

## v1.6.65

- 电脑端待办 DDL、日程开始、日程结束输入区增加卡片式日期时间预览，输入完整后显示大号时间和日期 / 周几，向手机端时间选择卡片靠拢
- 电脑端日期时间预览会在输入不完整或非法日期时回到空状态，避免 `2月31日` 这类日期被浏览器自动滚到其他日期造成误导
- 版本号升级到 `1.6.65` / `versionCode 137`

## v1.6.64

- 手机端待办编辑器的 DDL 日期行会在公历日期后追加农历括号，例如 `5月13日（农历三月廿七） · 周三`
- 手机端日程编辑器的开始 / 结束时间卡片，以及全天日程的开始 / 结束日期卡片，统一在公历日期后显示农历括号
- 这补齐了农历需求中的轻量日期选择反馈：不改系统 DatePicker 滚轴，但选中公历日期后能立即看到对应农历日期
- 版本号升级到 `1.6.64` / `versionCode 136`

## v1.6.63

- 电脑端已有日程卡片点击恢复为直接打开编辑弹窗，并通过 Node DOM 模拟验证标题、编辑 ID 和农历循环字段都能正确填充
- 电脑端待办 / 日程循环类型增加“每年同农历月日”选项，桌面同步 API 可提交并保存该循环类型
- 手机端待办和日程编辑器增加“每年同农历月日”循环选择与摘要；循环预览和实际实例生成会按起始日期对应的农历月日逐年映射到公历日期
- `LunarCalendar` 增加按年份反查同农历月日的能力，农历年度循环可用于农历生日等场景；农历滚轴日期选择器仍待后续实现
- 电脑同步状态进一步加固：设置关闭时，即使底层服务状态短暂滞后，状态对象也会强制返回未运行和空地址，避免继续显示旧局域网地址
- 每日看板普通日程行继续保持无外层线框，并收紧左侧颜色竖线间距；正在进行日程保留更轻的金色边框 / 内侧高光
- 日历顶部月份标题增加最小宽度保护，右侧操作按钮进一步压缩，降低“2026年5月”被按钮挤没的概率
- 版本号升级到 `1.6.63` / `versionCode 135`

## v1.6.62

- 新增 `LunarCalendar` 农历标签工具，基于 Android ICU 的 `ChineseCalendar` 将公历日期转换为农历月日标签
- 日历三日 / 单日顶部日期栏增加农历小字，便于在时间轴中同时看到公历和农历日期
- 月视图日期格增加农历标签，初一显示农历月份，其余日期显示农历日名
- 列表视图的周条和每日分组左侧日期增加农历标签
- 本轮只完成农历显示基础；农历日期选择器、农历生日循环 / 农历年度重复规则仍待后续实现
- 版本号升级到 `1.6.62` / `versionCode 134`

## v1.6.61

- 电脑端待办编辑的提醒时间改为手机端同款混合语法输入，不再只能填写单个提醒日期时间
- 电脑端日程编辑的提醒时间同样支持 `5,15,16:30,05-10 15:00,2026-05-10 14:30` 这类混合输入，并统一转换为提前分钟偏移
- 电脑同步 API 补齐待办 `reminderOffsetsMinutes` 读写，电脑端创建 / 修改待办时可以真正保存多个提醒点
- 提醒输入会阻止晚于 DDL 或日程开始时间的非法值，并在电脑端输入框上标红提示，不继续提交错误数据
- 通过 Node 模拟验证混合提醒语法解析：分钟、当天时间、当年日期时间和完整日期时间都能转换为正确偏移，晚于目标时间会报错
- 版本号升级到 `1.6.61` / `versionCode 133`

## v1.6.60

- 电脑端待办时间轴取消卡片内联“编辑 / 完成 / 取消 / 删除”按钮，改为点击待办卡片先打开详情预览
- 新增电脑端待办详情预览弹窗，展示颜色块、标题、分组、DDL、循环、提醒和备注，并在预览底部提供完成、取消、删除和编辑入口
- 电脑端日程卡片点击不再直接进入编辑表单，改为先打开日程详情预览；预览中展示颜色块、标题、时间、重复、地点、提醒和备注
- 日程预览右上角提供删除和编辑入口，删除继续走 PaykiTodo 应用内危险确认弹窗
- 通过 Node 模拟浏览器分别验证日程和待办的“点击卡片 -> 打开预览 -> 点击编辑 -> 打开编辑弹窗并填充字段”链路
- 版本号升级到 `1.6.60` / `versionCode 132`

## v1.6.59

- 桌面端网页资源版本参数改为运行时跟随 APK 版本号，并在电脑端左侧品牌区显示当前 Web 资源版本，便于判断浏览器是否仍在访问旧 APK 输出的页面
- 通过 Node 模拟浏览器点击验证新版桌面端已有日程卡片点击链路：点击卡片会打开日程编辑弹窗，且 `recurrenceWeekdays` 为字符串时不会阻断弹窗打开
- 电脑同步前台服务增加关闭状态自停保护：如果系统因 `START_STICKY` 重新拉起服务，但设置中电脑同步已关闭，会立即停止服务与本地同步协调器
- 日历当前时间刷新继续优化：顶层日历不再每 30 秒整体重组，当前时间 tick 下沉到时间轴和当前红线组件，顶层日期只在跨天时更新
- 每日看板普通日程行去掉非进行中底色和外框，并进一步收紧左侧颜色竖线与行块左边缘距离；只有正在进行的日程保留金色描边
- 日历头部改为月份标题占左侧可用空间、操作按钮固定在右侧，降低“2026年5月”被按钮挤没的概率
- 设置页电脑同步摘要在关闭时显示“未开启”，不再暗示仍有局域网控制台；使用说明入口继续直接打开 Wiki，不再保留中间面板
- 去掉手机端日程编辑起止时间区的一句泛泛说明文案，减少无必要 UI 注释
- 版本号升级到 `1.6.59` / `versionCode 131`

## v1.6.57

- 电脑端删除待办、日程和历史项目时，不再使用浏览器原生 confirm，统一改为 PaykiTodo 应用内危险操作确认弹窗
- 新确认弹窗包含红色危险图标、确认文案、取消和删除按钮，视觉上更接近手机端危险操作底部弹窗
- 桌面端资源版本参数升级到 `v=1.6.57`
- 版本号升级到 `1.6.57` / `versionCode 129`
## v1.6.56

- 电脑端待办编辑补齐 no-DDL 开关，关闭后禁用 DDL、提醒和循环字段，并向手机端提交无 DDL 待办
- 编辑已有无 DDL 待办时，电脑端会自动关闭 DDL 区块；编辑循环待办时禁用 DDL 开关，避免循环任务被改成无 DDL
- 电脑端日程 / 待办编辑弹窗去掉顶部泛泛说明副标题
- 桌面端资源版本参数升级到 `v=1.6.56`
- 版本号升级到 `1.6.56` / `versionCode 128`
## v1.6.55

- 手机端日程 / 待办编辑底部面板去掉泛泛说明副标题，避免“这个页面能设置哪些字段”这类无必要文案继续占据首屏
- 设置页去掉提醒链路测试、默认延后时长和关于页使用说明入口里的重复解释文案，保留真正影响权限、备份和风险判断的说明
- 电脑端网页控制台增加轻量交互动效：卡片进入、标签切换、弹窗背景淡入和按钮 hover / active 反馈
- 电脑端动画加入 `prefers-reduced-motion` 降级，系统要求减少动态效果时自动关闭动画和过渡
- 桌面端资源版本参数升级到 `v=1.6.55`
- 版本号升级到 `1.6.55` / `versionCode 127`
## v1.6.54

- 修复电脑端点击已有日程后表单已经填充但弹窗仍隐藏的问题：快照中的 `recurrenceWeekdays` 可能是字符串，前端原先直接调用 `.join()` 会抛错并阻断 `openModal()`
- 电脑端待办和日程编辑统一使用 `csvValue()` 兼容数组 / 字符串形式的循环星期字段
- 桌面端资源版本参数升级到 `v=1.6.54`
- 已通过 Edge DevTools Protocol 模拟浏览器点击验证：使用手机真实快照数据点击已有日程后，编辑弹窗可见且字段已填充
- 版本号升级到 `1.6.54` / `versionCode 126`
## v1.6.53

- 电脑端网页资源增加版本参数，并在本地同步服务响应中加入 no-store / no-cache，降低浏览器继续执行旧 JS 导致点击日程无效的概率
- 电脑端日程时间轴改为事件委托处理点击，全天日程恢复为每天列顶部的轻量胶囊入口，点击定时日程和全天日程都能进入编辑弹窗
- 手机端电脑同步设置在未启用或服务未运行时不再显示局域网访问地址，避免无连接状态下仍暴露历史地址
- 每日看板普通日程去掉外层同色边框，只保留正在进行日程的金色高亮；同时收紧日程左侧色条与卡片边缘的距离
- 日历顶部月份标题改为标题行与按钮行分离，避免“2026年5月”被右侧按钮挤到看不见；抽屉与窗口标题统一为“每日看板”
- 版本号升级到 `1.6.53` / `versionCode 125`
## v1.6.52

- 修复电脑端日程时间轴点击日程卡片无法打开编辑弹窗的问题：事件 ID 查找改为字符串 / 数字兼容匹配
- 日程卡片点击时阻止事件继续冒泡到空白时间轴，避免误触发新增日程逻辑
- 版本号升级到 `1.6.52` / `versionCode 124`
## v1.6.51

- 每日看板今日日程区现在固定展示“明天”段落；如果明天没有日程，会显示“明天暂无日程”，避免有今日日程但明天空白时逻辑断层
- 电脑端日程时间轴去掉事件卡片上的“编辑 / 删除”按钮，改为点击日程卡片进入编辑与删除入口
- 电脑端日程时间轴展示色改为优先使用分组颜色，和待办颜色逻辑保持一致
- 电脑端删除日程时间轴上方独立的全天日程栏，避免占用过多空间；顶部副标题也不再显示全天统计
- 版本号升级到 `1.6.51` / `versionCode 123`

## v1.6.50

- 修复工作模式下的强震动兜底：即使单条待办 / 日程原本关闭了震动，工作模式开启时也会强制使用强震动，避免出现“静音但无震动”的无感提醒
- 工作模式仍保持静音、不外放提醒声音，并继续让日程提醒走全屏 / 无障碍兜底链路
- 同步 README、TODO、Wiki 和 current docs，明确工作模式是“静音 + 强制强震动 + 全屏/无障碍兜底”
- 版本号升级到 `1.6.50` / `versionCode 122`

## v1.6.49

- 恢复“工作模式”的原定语义：开启后默认不外放提醒声音，改用更强震动，并继续让日程提醒走全屏 / 无障碍兜底链路
- 新建提醒的默认响铃状态再次跟随工作模式：工作模式开启时默认关闭响铃、保留震动强化
- 设置页保留 `1.6.48` 已完成的紧凑下拉与滑条输入，不回退为大按钮组选项
- 更新项目 UI 规则文档，明确工作模式是静音强提醒模式，并补充提交信息需要写清具体行为变化
- 版本号升级到 `1.6.49` / `versionCode 121`

## v1.6.48

- 设置页继续压缩视觉噪音：播放通道、日历周起始日、日历默认提醒方式由大按钮组选项改为紧凑下拉选择
- PaykiTodo 内部音量和临时系统通道提升目标改为 0-100 滑条 + 数值输入框，不再只提供 `±10%` 按钮
- “课堂 / 上班模式”更名为“工作模式”，行为改为保留响铃强度、增强震动，并继续让日程提醒走全屏 / 无障碍兜底链路
- 电脑同步从高级设置移回常用设置；高级设置保留提醒链路诊断、数据与备份、崩溃日志这类维护项
- 新增项目 UI 设计规则文档，明确禁止把互斥选项做成一排大按钮污染界面
- 版本号升级到 `1.6.48` / `versionCode 120`

## v1.6.47

- 新增提醒声音策略：支持选择闹钟、无障碍辅助、通知、媒体四种播放通道
- 新增 PaykiTodo 内部提醒音量百分比，播放内置音或系统提示音时按 App 内部音量控制
- 新增课堂 / 上班模式：默认不外放提醒声音，改用更强震动，并让日程提醒也走全屏 / 无障碍兜底链路
- 新增高级选项“提醒时临时提升系统通道音量”，可在提醒播放期间尝试提升所选系统通道音量，结束后恢复原值，默认关闭
- 设置页重整为“常用设置 / 高级设置”，将提醒链路诊断、数据与备份、电脑同步、崩溃日志归入高级设置
- Wiki 更新声音策略、临时音量提升、课堂 / 上班模式和新设置分区说明
- 版本号升级到 `1.6.47` / `versionCode 119`

## v1.6.46

- 内置 Wiki 使用说明改为小屏也保持左侧目录、右侧正文的布局，避免目录按钮堆在正文上方并需要翻到页面底部查看内容
- 每日看板今日日程区区分“今天没有日程”和“今天有日程但都已结束”，后者显示完成提示文案
- 每日看板今日日程标题计数改为今天总日程数，而不是只统计尚未结束的日程
- 抽屉菜单顶部应用图标改为圆形裁切并填满圆形底板，减少深色模式下白色圆角矩形图标的割裂感
- 版本号升级到 `1.6.46` / `versionCode 118`

## v1.6.45

- 将电脑端 Web UI 从 `DesktopSyncWebAssets.kt` 的 Kotlin 大字符串拆分到 `app/src/main/assets/desktop-web/`
- `DesktopSyncWebAssets.kt` 改为轻量 assets 读取器，并保留资源加载失败时的最小 fallback 页面
- `DesktopSyncCoordinator` 继续由手机端局域网服务提供 `/index.html`、`/app.css`、`/app.js`，但资源来源改为 APK assets
- 新增 `docs/current/DESKTOP_WEB_ARCHITECTURE.md`，说明电脑端 UI 为什么会随 APK 打包、当前架构边界和未来重构建议
- 版本号升级到 `1.6.45` / `versionCode 117`

## v1.6.44

- 精修电脑端网页控制台的待办 / 日程编辑弹窗，改为更接近手机端底部弹窗的结构：顶部左侧取消、居中标题、右侧保存
- 电脑端编辑表单字段卡片化，标题输入突出显示，DDL / 提醒 / 循环 / 铃声震动等字段分组更清晰
- 优化电脑端待办时间轴、全天日程和定时日程卡片的边框、留白、按钮密度与 hover 反馈
- 修复电脑端编辑弹窗中的删除按钮隐藏样式，使新增模式下不会露出删除入口
- 版本号升级到 `1.6.44` / `versionCode 116`

## v1.6.43

- 电脑端网页控制台新增已有待办编辑能力，可修改标题、备注、DDL、提醒时间、分组、循环、铃声和震动
- 电脑端待办卡片新增编辑入口，并在编辑弹窗内支持保存修改或删除待办
- 电脑端日程与全天日程卡片补齐编辑按钮，避免只能依赖点击卡片空白区域进入编辑
- 新增 `PUT /api/todos/{id}` 桌面同步接口，编辑后会重新调度或关闭提醒
- 版本号升级到 `1.6.43` / `versionCode 115`

## v1.6.42

- 启动页改用透明背景图标资源 `ic_launcher_art_transparent`，去掉启动页中央图标的白色方块背景
- 自定义延后输入取消 180 分钟上限，只要求目标时间晚于当前时间
- 待办延后目标晚于当前 DDL 时，会同步把该待办 DDL 改到延后目标，并把下一次提醒固定到该时间
- 更新提醒页、输入帮助和 Wiki 的自定义延后说明
- 版本号升级到 `1.6.42` / `versionCode 114`

## v1.6.41

- 统一每日看板日程卡片的左侧颜色竖线位置，正在进行与未开始日程保持同一列对齐
- 未开始日程新增与左侧竖线同色的细边框，避免只露出侧边弧线
- 正在进行日程取消大面积黄色铺底，只保留金色边框和极轻的内侧高光，提升文字可读性
- 版本号升级到 `1.6.41` / `versionCode 113`

## v1.6.40

- 修复待办批量导入的 DDL 解析缺口：`16:30` 现在会被识别为今天 16:30
- 待办批量导入 DDL 同时兼容中文冒号，例如 `16：30`
- 更新待办批量导入弹窗、输入帮助和 Wiki 示例，明确 DDL 支持缺省当天日期
- 版本号升级到 `1.6.40` / `versionCode 112`

## v1.6.39

- 每日看板移除“批量添加待办 / 批量添加日程”操作入口，保持看板只用于查看今日信息
- 我的任务页不再显示顶部双批量按钮，右下角与新建待办按钮并列提供“批量待办”入口
- 每日看板今日日程的左侧颜色条改为跟随右侧文字内容高度，避免色条和文字块高度不一致
- 版本号升级到 `1.6.39` / `versionCode 111`

## v1.6.38

- 重新处理图片版应用图标：整体图案内缩到安全区域，避免桌面 adaptive mask 裁切得过满
- 将 `ic_launcher_art` / `ic_launcher_art_v2` / `ic_launcher_art_dark` 合成为不透明纯白背景，P 内圈等透明区域也落成白色
- 启动器 adaptive icon 背景色改为纯白 `#FFFFFF`
- 版本号升级到 `1.6.38` / `versionCode 110`

## v1.6.37

- 修复启动器图标被误接回旧矢量 mark 的问题
- 启动器普通图标和圆形图标的 adaptive icon 前景重新改为图片版 `ic_launcher_art`
- 删除旧的 `ic_payki_mark` / `ic_payki_mark_monochrome` / `ic_launcher_foreground` / `ic_launcher_monochrome` 资源，避免后续再次误用
- 版本号升级到 `1.6.37` / `versionCode 109`

## v1.6.36

- 待办提醒、日程提醒、待办批量、日程批量和自定义延后输入框旁新增圆形问号帮助按钮
- 新增共享输入语法帮助弹窗，说明提醒时间、待办批量、日程批量和延后时间的填写规则
- 更新内置 Wiki 到 1.6.36，补充统一提醒时间写法、待办批量逗号语法、日程批量 `Remind=` 和自定义延后说明
- 无障碍提醒浮层的自定义延后行增加问号提示按钮
- 版本号升级到 `1.6.36` / `versionCode 108`

## v1.6.35

- 待办批量添加改为轻量逗号语法：`DDL时间,任务名称,提醒时间`，默认使用默认分组、响铃和震动设置
- 待办批量添加的提醒时间只需填写一个提醒点，支持 `5`、`16:30`、`05-10 15:00`、`2026-05-10 14:30`
- 日程批量导入的 `Remind=` 字段接入同一套提醒时间识别逻辑，不再只局限于 `15m / 1h / 2d`
- 全屏提醒页和无障碍提醒浮层的“自定义延后”输入支持分钟数或具体未来时刻
- 版本号升级到 `1.6.35` / `versionCode 107`

## v1.6.34

- 待办与日程提醒时间统一改为文本输入：支持 `5,15,16:30,05-10 15:00,2026-05-10 14:30` 这种逗号分隔多提醒写法
- 提醒输入会校验每一项：晚于 DDL / 日程开始时间、格式非法或新建时已过期都会标红，并禁用保存按钮
- 普通待办正式接入 `reminderOffsetsCsv` 多提醒链路，避免待办只能保存和派发单个提醒点
- 新增待办批量导入弹窗，支持按行导入 DDL、标题、备注、分组、响铃震动和多提醒配置
- 每日看板 / 我的任务增加显性的“批量添加待办 / 批量添加日程”按钮，日历页顶部也新增独立“批量”按钮
- 版本号升级到 `1.6.34` / `versionCode 106`

## v1.6.33

- 修复内置 Wiki 左侧目录点击无响应的问题：WebView 对本地 Wiki 页面启用 JavaScript，并限制本地文件跨源访问
- 设置页“使用说明”改为直接打开 Wiki，“提示音”改为直接打开系统通知提示音选择器，不再先进入二级小界面
- 电脑端日程全天栏支持跨多天全天事件横向连成一条，而不是每天重复一个小块
- 启动器 adaptive icon 前景重新接回安全区矢量 PaykiTodo mark，避免误回退到旧位图图标链路
- 版本号升级到 `1.6.33` / `versionCode 105`

## v1.6.32

- 每日看板待办区纳入已错过但仍处于活动态的待办，避免错过任务在看板中消失
- 删除确认底部弹窗升级为统一危险操作样式：红色删除图标、不可恢复提示、摘要卡片、红色主按钮与取消按钮
- 版本号升级到 `1.6.32` / `versionCode 104`

## v1.6.31

- 每日看板今日日程不再显示已经结束的定时日程
- 每日看板中正在进行的日程新增金黄色描边与轻微光晕，高亮当前正在发生的安排
- 日历时间轴预添加日程状态新增取消路径：长按空白时间轴可取消，点开已有日程也会清空预添加草稿
- 版本号升级到 `1.6.31` / `versionCode 103`

## v1.6.30

- 待办卡片正文点击与完成复选框拆分，修复今日看板中打开待办预览后返回会误标记完成的问题
- 待办预览改为与日程预览一致的底部弹窗风格，提升今日看板与我的任务页面的交互一致性
- 手机端与电脑端 Web 的删除入口补充确认；模板删除、分组删除、日程删除、历史项删除等不再直接执行不可逆删除
- 日程提醒响完后保留原本设置过的提醒提前量，预览页继续显示已配置的提醒时间
- 电脑同步常驻通知新增点击跳转，点击后直接进入应用内“设置 -> 电脑同步”分区
- 版本号升级到 `1.6.30` / `versionCode 102`

## v1.6.13

- 修复日历横向切到离今天较远的日期后，左侧当前时间与红线一起消失的问题
- 当前时间标签现在始终显示在左侧时间轴；当今天列不在当前视口内时，日程区仍会持续绘制整条当前时间红线
- 版本号升级到 `1.6.13` / `versionCode 85`

## v1.6.12

- 启动器图标前景不再复用整张成品位图，改为独立的安全区矢量 logo，修正不同桌面形态下的放大与裁切异常
- 单色 themed icon 同步改成与彩色启动器图标一致的 PaykiTodo logo 轮廓
- 版本号升级到 `1.6.12` / `versionCode 84`

## v1.6.11

- 升级版本号，便于在保留本地数据的前提下直接覆盖安装验证修复结果
- 当前修复内容包含启动闪退风险规避与安装页图标尺寸调整
- 版本号升级到 `1.6.11` / `versionCode 83`

## v1.6.10

- 日历当前时间文字回到左侧时间轴，红线仍保留在日程区
- 启动器图标链路切到当前 PaykiTodo 图标资源，通知小图标同步更新
- 首页新增每日看板默认入口，今日待办与今日日程集中展示
- 看板背景图区分浅色 / 深色资源，启动页与抽屉图标视觉同步更新
- 补充 release signing 模板与仓库内新会话交接文档
- 版本号升级到 `1.6.10` / `versionCode 82`

## v1.6.9

- 当前代码版本已提升到 `1.6.9` / `versionCode 81`
- 本轮工作树重点集中在图标资源链路、看板/日历打磨与文档清账
- 当前仓库仍处于未完全提交的进行中状态，不应误认为已经形成完整稳定发布

## v1.6.7

- 重新升版出包，避免安卓侧同版本覆盖安装造成的旧包缓存混淆
- 版本号升级到 `1.6.7` / `versionCode 79`

## v1.6.5

- 每日看板欢迎语改为可折叠，去掉手动刷新按钮
- 日历红线时间标签改为在非今天列也持续可见
- 更换全局壁纸与启动页配色，提升浅色背景下的文字可读性
- 调整启动器与通知栏图标资源细节
- 升级版本号到 `1.6.5` / `versionCode 77`

## v1.6.4

- 新增每日看板界面，欢迎语降权，突出今日待办与今日日程
- 日历继续精修：单日视图支持左右滑切天，头部进一步压缩
- 升级版本号到 `1.6.4` / `versionCode 76`

## v1.6.3

- 继续精修日历视图交互与视觉完成度
- 升级版本号到 `1.6.3` / `versionCode 75`

## v1.6.2

- 修复安装页与桌面启动图标错误显示为异常黑底白条的问题
- 启动器图标改为新的矢量自适应资源，避免继续依赖损坏位图
- 抽屉菜单头部图标改为与启动器一致的新图标样式
- 版本号升级到 `1.6.2` / `versionCode 74`

## v1.6.1

- 修复电脑端新增“过去时间”的日程时，会在手机端错误触发提醒的问题
- 电脑端网页控制台新增循环规则、循环截止日期、提醒方式、提醒时间点等高级字段
- 电脑端创建待办/日程成功后，现在会自动清空左侧表单
- 调整网页控制台布局，左侧导航、顶部工具栏、编辑表单改为更稳定的固定式布局
- 版本号升级到 `1.6.1` / `versionCode 73`

## v1.6.0

- 新增手机端局域网网页控制台，实现 PaykiTodo 电脑同步第一版
- 设置页新增“电脑同步”入口，可开启服务、查看局域网地址、查看和重置访问密钥
- 电脑浏览器现可直接查看手机中的待办与日程，并执行基础新增与处理操作
- 新增同步前台服务，支持应用重启、设备重启后的自动恢复
- 内置 Wiki 与 README 同步补充电脑同步的完整使用说明
- 版本号升级到 `1.6.0` / `versionCode 72`

## v1.4.9

- 修复锁屏场景下日程全屏提醒链路，优先走无障碍全屏浮层兜底
- 修复日程“直接设置提醒时刻”会被相对提前时间覆盖的问题
- 修复编辑已有日程时仍可把提醒时间改到过去的问题
- 提升启动页、设置页、抽屉菜单、历史记录与提醒界面的整体完成度
- 完成日历、批量导入、备份、崩溃日志、关于页等模块的整理与联调

## v1.1.x Early Milestones

- 默认延后时长选择器从旧方案切换到可控实现
- 启动页、关于页、抽屉菜单等基础体验持续补齐
- 辅助功能全屏覆盖提醒兜底链路接入
