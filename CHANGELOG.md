# Changelog

PaykiTodo 采用语义化倾向的应用版本号和递增 `versionCode`。本文件面向用户记录重要变化，不再保存每一次内部调试重建的完整流水账；完整历史可通过 Git commit、tag 和 `docs/archive/` 追溯。

## v1.13.45

发布日期：2026-06-02（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端待办快速预览底部把 `取消并归档` 提升为独立整行操作卡，不再和 `修改` / `删除` 挤在同一排小按钮里。
- `取消并归档` 继续进入历史记录；`删除` 仍是不进入历史的硬删除路径，预览小界面里二者更容易区分。
- 电脑端 Web 规划台的识别 / 导入预览中，每周循环周几从手写数字改为周几多选，并在非每周循环时自动隐藏和清空周几。

### 构建

- `versionName = 1.13.45`
- `versionCode = 293`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.44

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑端 Web 待办编辑器的每周循环周几从手写 `1,3,5` 改为紧凑周几多选，不再要求用户记数字格式。
- 电脑端 Web 日程编辑器同步使用周几多选，并只在循环规则为 `每周` 时显示，避免非每周循环时误填无效周几。
- 周几多选仍保存为现有循环字段，不新增桌面同步接口或数据库迁移。

### 构建

- `versionName = 1.13.44`
- `versionCode = 292`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.43

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端待办快速预览的底部固定操作区新增 `取消归档` 按钮，不再只依赖顶部文字或正文提示卡。
- `取消归档` 继续走历史归档路径；`删除` 仍为不进入历史记录的硬删除路径，二者在预览小界面中保持并列且语义明确。

### 构建

- `versionName = 1.13.43`
- `versionCode = 291`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.42

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑端 Web 待办编辑器新增明确的 `启用提醒` 开关，不再要求用户靠清空提醒时间来关闭提醒。
- 电脑端 Web 日程编辑器新增明确的 `启用提醒` 开关，关闭后会禁用提醒时间、提醒方式、铃声和震动相关字段。
- 待办新建默认保持 5 分钟提醒；日程新建默认不启用提醒，和手机端默认行为一致。
- 保存时关闭提醒会传入空提醒列表，继续复用现有桌面同步接口和数据库字段。

### 构建

- `versionName = 1.13.42`
- `versionCode = 290`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.41

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑端 Web 日程编辑器的标题输入框改为多行文本区，支持像手机端一样输入换行标题。
- 日程标题换行会继续通过现有保存接口写入同一个 `title` 字段，不新增数据库迁移。

### 构建

- `versionName = 1.13.41`
- `versionCode = 289`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.40

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端待办快速预览在标题下方新增醒目的 `取消并归档` 操作卡，不再只依赖顶部文字或底部操作区。
- 手机端待办快速预览顶部操作从 `取消待办` 改为 `取消并归档`，明确取消会进入历史记录，不等同于删除。
- 电脑端待办详情预览同步新增正文归档操作卡，并将顶部 / 底部取消按钮统一为 `取消并归档`。
- 取消仍走归档历史路径；删除仍为硬删除路径，二者业务语义不混用。

### 构建

- `versionName = 1.13.40`
- `versionCode = 288`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.39

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑端 Web 待办编辑器补齐手机端已有的 `闹钟模式` 开关。
- 电脑端 Web 待办编辑器补齐手机端已有的 `仅提醒，不在看板显示` 开关。
- 新增和编辑待办时会正确回填、保存这两个字段；未设置 DDL 时会自动关闭这些依赖 DDL 的选项。

### 构建

- `versionName = 1.13.39`
- `versionCode = 287`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.38

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 待办卡片长按快捷菜单里的取消操作改为 `取消待办（归档）`，和详情预览、提醒界面保持一致。
- 取消确认弹窗标题改为 `取消待办（归档）`，确认按钮改为 `取消并归档`，进一步区分取消归档与硬删除。
- 业务逻辑不变：取消进入历史记录，删除仍是不进入历史记录的硬删除。

### 构建

- `versionName = 1.13.38`
- `versionCode = 286`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.37

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 提醒全屏界面里的取消操作改为 `取消待办（归档）`，并使用橙色归档语义，不再像删除一样使用危险红色。
- 无障碍兜底提醒层同步改为 `取消待办（归档）`，取消后的提示明确为“已取消并归档”。
- 业务逻辑仍保持取消进入历史记录、删除不进入历史记录，避免提醒界面和快速预览的语义不一致。

### 构建

- `versionName = 1.13.37`
- `versionCode = 285`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.36

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端待办详情快速预览顶部重新提供醒目的 `取消待办` 操作，底部继续保留 `取消待办（归档）` 说明；从看板、搜索、规划台关联项、通知或小组件进入预览时，都不需要误用删除来取消待办。
- 待办每周循环在用户尚未手动选择周几时，会随 DDL 日期自动同步默认星期，避免“把 DDL 改到 5 月 25 日，但循环实例仍按旧星期生成”的错觉。
- 用户一旦手动调整每周循环的星期选择，后续 DDL 日期变化不会覆盖显式选择。

### 构建

- `versionName = 1.13.36`
- `versionCode = 284`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.35

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端 `设置 → 电脑同步` 现在会区分显示 `等待电脑端输入访问密钥` 和 `已连接电脑端`，不再只笼统显示“服务正在运行”。
- 电脑同步状态会在同步开关开启期间周期刷新，已连接 / 断开 / 自动关闭倒计时能在设置页持续更新。
- 电脑同步状态接口新增授权心跳时间、连接状态和自动关闭剩余秒数；电脑端网页仍保持兼容，手机端可据此说明“未输入密钥不算连接”和“断开后约多久自动关闭”。

### 构建

- `versionName = 1.13.35`
- `versionCode = 283`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.34

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端待办快速预览底部现在固定显示操作区，`取消待办（归档）` 不再随详情内容滚动，也不再需要从顶部小按钮或长按菜单里寻找。
- 预览顶部只保留返回，底部固定区集中展示取消归档、删除、修改、恢复等动作；取消仍会进入历史记录，删除仍是不进入历史的硬删除。

### 构建

- `versionName = 1.13.34`
- `versionCode = 282`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.33

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 进行中日程通知的结束闹钟现在会重新读取最新日程状态；如果日程已被延长或改期到仍在进行，不会被旧的结束广播提前清掉通知。
- 只有日程确实结束、已完成 / 已取消、被删除或不再是日程时，结束广播才会清理进行中通知。

### 构建

- `versionName = 1.13.33`
- `versionCode = 281`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.32

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 修正提醒方式选择不生效的问题：待办 / 日程选择 `通知栏提醒` 时，不再被服务层无条件强制拉起全屏提醒界面。
- `全屏界面提醒`、`闹钟模式`、`工作模式` 仍会进入强提醒 / 全屏链路，保留不能错过的强提醒能力。

### 构建

- `versionName = 1.13.32`
- `versionCode = 280`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.31

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端待办详情快速预览中，`取消待办（归档）` 现在作为独立醒目的操作卡显示，说明会停止提醒并进入历史记录，避免用户误以为只能删除。
- 电脑端 Web 待办详情预览同步强化 `取消待办（归档）` 操作样式，和删除按钮明确区分；取消仍走归档/历史记录流程，删除仍是不进入历史的硬删除。

### 构建

- `versionName = 1.13.31`
- `versionCode = 279`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.30

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端规划台识别预览的批量设置中，`全部加入倒数日` 现在同时作用于已勾选的日程候选和带 DDL 的待办候选；此前只能批量处理待办，和单条日程候选可开启倒数日的能力不一致。

### 构建

- `versionName = 1.13.30`
- `versionCode = 278`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.29

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑端 Web 规划台识别预览中的日程候选补齐 `打卡追踪` 开关，和手机端规划台预览保持一致；勾选后导入的日程会启用事件打卡追踪。
- 该补丁复用已有 `checkInEnabled` 导入字段和后端导入链路，不新增数据库迁移。

### 构建

- `versionName = 1.13.29`
- `versionCode = 277`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.28

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端待办详情快速预览顶部操作改为直接显示 `取消待办`，不再使用容易误解的 `取消归档` 文案；底部仍保留 `取消待办（归档）`，明确取消会进入历史记录、删除不会进入历史记录。
- 从每日看板、搜索结果、规划台关联项、通知 / 小组件等入口打开未完成待办时，快速预览都保持可取消；只有已完成或已取消的历史待办隐藏取消 / 删除并显示恢复入口。
- 电脑端 Web 待办详情预览顶部新增 `取消待办` 操作，用户不用滚到底部也能从快速预览执行归档取消。

### 构建

- `versionName = 1.13.28`
- `versionCode = 276`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.27

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 日程全屏提醒页和无障碍覆盖提醒中点击 `我知道了` / `签到` 时，不再清掉进行中日程通知的开始 / 结束闹钟；确认提醒后仍会保留或重排 `进行中日程` 常驻通知，避免日程开始后通知栏没有持续提示。
- 进行中日程通知的通知 ID 改为使用完整事项 ID 哈希，不再只取 `eventId % 10000`，降低大量日程下通知互相覆盖或错误取消的风险。
- 当前文档同步修正延后提醒语义：当 DDL 已过期或不晚于下一次提醒时，延后提醒会把 DDL 推到下一提醒后 1 分钟，而不是继续沿用旧的“只改提醒不改 DDL”描述。

### 构建

- `versionName = 1.13.27`
- `versionCode = 275`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.26

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 手机端从搜索结果、规划台关联事项、通知 / 小组件等外部入口打开待办时，现在统一进入待办详情快速预览，而不是直接跳进编辑器或依赖当前列表命中；预览里可以直接执行取消归档、删除、修改和恢复。
- 待办详情快速预览顶部把归档动作改为明确的 `取消归档` 文字按钮，并继续保留底部 `取消待办（归档）`，避免把关闭弹窗、删除和取消归档混淆。
- 规划台点击已关联待办现在先打开待办详情预览；点击已关联日程仍跳转到日历详情预览。

### 构建

- `versionName = 1.13.26`
- `versionCode = 274`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.25

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 内置 Wiki 的待办预览说明同步更新为当前行为：左侧圆圈只负责完成，详情预览提供 `取消待办（归档）`、删除和修改。
- 使用说明明确区分取消和删除：取消会进入历史记录；删除是硬删除，不进入历史记录。
- 使用说明补充循环待办删除语义：仅当前删除会写入跳过记录，当前及之后 / 全部删除会按范围硬删除。

### 构建

- `versionName = 1.13.25`
- `versionCode = 273`
- 数据库版本保持 `27`，不新增数据迁移。

## v1.13.24

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 待办详情快速预览现在把取消动作明确标为 `取消待办（归档）`，并在执行前说明取消会进入历史记录，避免用户把关闭、删除和取消混在一起。
- 电脑端 Web 待办预览同样补充取消确认，文案明确取消会归档、删除会直接移除且不进入历史记录。
- 循环待办的删除范围不再误走取消归档逻辑；`当前及之后` / `全部` 删除会真正硬删除对应实例，不再把它们写入历史记录。
- 循环待办“只删除当前一次”新增循环实例跳过记录，既不会进入历史记录，也不会被循环模板在后续补生成回来。
- 备份 / 恢复会保留循环实例跳过记录，避免恢复备份后已删除的单次循环待办重新出现。

### 构建

- `versionName = 1.13.24`
- `versionCode = 272`
- 数据库版本升级到 `27`，新增 `recurring_instance_skips` 表用于保存循环系列的单次删除例外。

## v1.13.23

发布日期：2026-06-01（本地调试构建，尚未创建 GitHub Release）

### 修复

- 待办的快速预览小界面现在直接提供 `取消待办`，不再只让用户通过删除完成归档操作；取消和删除保持分离，前者会进入历史记录，后者不会。

### 构建

- `versionName = 1.13.23`
- `versionCode = 271`
- 数据库版本保持 `26`，不新增数据迁移。

## v1.13.21

发布日期：2026-05-25（本地调试构建，尚未创建 GitHub Release）

### 修复

- 规划台解析器现在会让行内显式日期优先于规划文档日期：在 5 月 20 日文档中写 `5.29 【DDL】...` 或 `5.29【DDL】...` 会生成 5 月 29 日的待办，不再错误落到文档日期。
- 规划台自然 DDL 标题清理会移除 `【DDL】` / `[DDL]` / `截止` 等标记，同时保留用户写的其他强调标签，例如 `【紧急】`。
- 循环待办 / 循环日程选择“整个循环系列”编辑时，如果用户把起始日期改到一个新日期，会按新日期重建整个系列；只有纯改时间、没有改日期时才回到原系列起点。
- 循环系列重建前会先删除旧循环模板，再写入新模板，避免旧模板的起始日期继续补生成旧的未来实例。

### 构建

- `versionName = 1.13.21`
- `versionCode = 269`
- 数据库版本保持 `26`，不新增数据迁移。

## v1.13.20

发布日期：2026-05-25（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑端 Web 规划台的大纲事项区现在是明确的主输入区：空文档提示可点击聚焦输入框，输入后按 Enter 会直接创建规划草稿，不再需要绕到 Markdown 兼容编辑区才能写内容。
- 电脑端 Web 规划台已有大纲节点改为可直接编辑的多行文本框，并把拖拽排序限制到专用拖拽手柄，避免整行拖拽状态干扰文字输入。
- 电脑端 Web 规划台在发布、识别、导入、刷新、顺延、撤销和切换文档前会先提交未完成的大纲输入，降低“刚输入的内容未参与识别/发布”的误操作风险。

### 改进

- 电脑端 Web 规划台补充主输入区、Markdown 兼容区、识别预览区的说明文案和按钮标题，明确“大纲事项用于日常输入，Markdown 兼容区用于粘贴旧文本/批量识别”。
- 大纲输入支持一次粘贴多行文本并拆成多条草稿；Shift+Enter 可在文本框内换行。
- 大纲节点变化后会在兼容区没有未保存手动编辑时同步刷新 Markdown 导出文本，避免识别读取旧内容。

### 构建

- `versionName = 1.13.20`
- `versionCode = 268`
- 数据库版本保持 `26`，不新增数据迁移。

## v1.13.19

发布日期：2026-05-25（本地调试构建，尚未创建 GitHub Release）

### 改进

- 待办列表预先按分组 ID 构建分组映射，并把解析后的分组传给可见卡片，减少大量事项滑动时每张卡片反复扫描分组列表的开销。
- 未来循环待办展开时最多直接渲染前 30 个实例，其余实例以折叠提示呈现，避免一个长循环系列把 `计划中` 列表一次性撑成大量可组合项。
- 日历单日/三日时间轴只为当前页面日期计算 timed event 分段和重叠排版，不再先为整个已加载事件窗口预排全部日期。
- 日历时间轴当前时间标记改为分钟级刷新，并把纵向渲染过扫描从 2 小时收窄到 1 小时，减少滑动过程中的重复重组和绘制。

### 审计

- 复核循环待办/循环日程删除、取消、范围编辑、模板截断和补生成路径：当前实例删除仍保留取消占位，`当前及之后` 仍使用原始循环锚点日期切分，本轮未发现新的仓库层循环数据语义错误。
- 新增循环待办展示分组测试，覆盖未来循环实例折叠成一个系列组并按 DDL 排序的行为。

### 构建

- `versionName = 1.13.19`
- `versionCode = 267`
- 数据库版本保持 `26`，不新增数据迁移。

## v1.13.18

发布日期：2026-05-25（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑端 Web 待办/日程详情预览里的删除、取消操作会识别循环事项，并默认按“当前及之后”范围提交，避免预览入口漏带范围参数导致未来循环实例继续存在。
- 倒数日 Widget 去掉待办条目上的无效勾选圆圈，预览布局同步移除旧圆圈占位和相关样式资源。
- 三日视图靠近日历窗口边界时保持三列日期显示，避免边缘页只剩两天。

### 改进

- 数据库升级到版本 26，并为活动待办、历史待办和日历范围查询新增运行期索引，降低事项很多时 Room 查询与排序成本。
- 待办列表的完成勾选改为轻量 Canvas 绘制；普通未完成文本不再注册划线动画用的文本布局回调，减少大量事项滑动时的行内绘制/测量开销。
- 待办三分区改为使用当天本地毫秒区间判断，避免每条事项都转换 `LocalDate`，并补充日期边界回归测试。
- 日历单日/三日时间轴按日期缓存 timed event 重叠排版，预计算时间段字段，并只在当前纵向视口附近渲染事件；月视图/列表视图不再承担时间轴排版成本。
- 今日看板 / 倒数日 Widget 的卡片背景、问候卡描边、日程卡 padding 和倒数日条目间距继续向应用内每日看板视觉靠拢。

### 构建

- `versionName = 1.13.18`
- `versionCode = 266`
- 数据库版本升级到 `26`，新增索引迁移 `MIGRATION_25_26`。

## v1.13.16

发布日期：2026-05-25（本地调试构建，尚未创建 GitHub Release）

### 修复

- 删除循环待办或循环日程的“仅当前”实例时保留取消占位，避免循环模板补齐机制在后续启动或刷新时把已删除实例重新生成。
- 循环实例补齐、当前及未来范围修改、取消、删除改为优先使用原始循环锚点日期判断，不再被用户单独移动过的 DDL / 开始时间误导。
- 仅修改循环系列的当前实例时保留原循环规则，并阻止在“仅当前”范围内变更循环规则；需要改规则时必须选择“当前及之后”或“整个循环系列”。
- 手机端删除循环待办时增加范围选择，避免误以为删除系列但未来实例仍继续出现。

### 改进

- 日历日视图 / 三日视图只组合和渲染当前纵向视口附近的 timed event，并按半小时滚动分桶减少滑动过程中的重复计算。
- 待办卡片改为轻量 `Surface`，移除高成本阴影绘制，并缓存单卡片分组解析结果，降低大量事项上下滑动时的测量和绘制压力。

### 构建

- `versionName = 1.13.16`
- `versionCode = 264`
- 数据库版本保持 `25`，不新增数据迁移。

## v1.13.15

发布日期：2026-05-24（本地调试构建，尚未创建 GitHub Release）

### 修复

- 修复循环日程按“整个循环系列”改为“不重复”时旧循环模板未删除的问题，避免后续启动或循环补生成时把已经改掉的未来日程重新生成。

### 改进

- 待办/看板状态的大列表分类、倒数日排序、公告解析改为后台派生计算，减少事项数量多时主线程被排序和解析阻塞的概率。
- 电脑同步状态读取从普通待办/日程列表刷新中拆出，避免每次列表变化都触发网络地址枚举和同步状态重算。
- 待办卡片左侧颜色条改为绘制层实现，移除列表项里的 intrinsic 高成本测量，改善大量事项上下滑动时的卡顿。
- 日历单日/三日时间轴只为当前页可见日期计算日程重叠排版，不再为整个已加载窗口预排所有 timed event，降低日历滑动和切页开销。

### 审计

- 复查循环任务生成、范围编辑、取消和删除链路：循环实例初始生成仍限制为 14 个实例/14 天前瞻，范围编辑和删除会通过模板截断或模板删除阻止后续补生成；本轮修复只改仓库逻辑，不新增数据库迁移。

### 构建

- `versionName = 1.13.15`
- `versionCode = 263`
- 数据库版本保持 `25`，不新增数据迁移。

## v1.13.14

发布日期：2026-05-24（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑同步服务改为持续检测授权桌面端心跳：电脑端未输入访问密钥，或曾连接后断开超过 5 分钟，手机端会自动关闭电脑同步开关。
- 电脑同步前台通知区分“等待连接”和“已连接”，自动关闭时会停止前台服务并移除通知。
- 电脑端 Web 连接成功后每 60 秒发送一次授权心跳，关闭网页、断网或电脑休眠后不再维持手机端同步服务。
- 手机端状态读取不再裸启动局域网 HTTP server；开关开启但服务未运行时会优先启动前台服务，避免出现可访问但通知栏没有同步服务的状态。

### 构建

- `versionName = 1.13.14`
- `versionCode = 262`
- 数据库版本保持 `25`，不新增数据迁移。

## v1.13.13

发布日期：2026-05-24（本地调试构建，尚未创建 GitHub Release）

### 修复

- 电脑端 Web 待办编辑器的标题输入改为多行输入框，支持输入和保存手机端已有的换行标题。
- 电脑端待办/日程列表与详情预览保留标题换行显示，不再把多行标题压成一行。
- 电脑端编辑循环待办或循环日程时新增“修改范围”，支持仅当前、当前及之后、整个循环系列。
- 电脑端把循环待办改为“不重复”时，默认按“当前及之后”处理，避免未来已生成的循环实例继续出现。
- 桌面同步后端 `PUT /api/todos/{id}` 与 `PUT /api/events/{id}` 接收循环修改范围，并在删除/取消接口中支持范围参数。

### 构建

- `versionName = 1.13.13`
- `versionCode = 261`
- 数据库版本保持 `25`，不新增数据迁移。

## v1.13.12

发布日期：2026-05-24（本地调试构建，尚未创建 GitHub Release）

### 修复

- 修正 Android 启动器图标资源链路：普通图标和圆形图标统一使用透明主图，不再引用旧的白底图标素材。
- 为 adaptive icon 补充 themed icon 的 `monochrome` 链路，降低桌面启动器把旧图标派生为单调回退图标的概率。
- 清理未被引用的旧 launcher 图标中间产物，避免后续维护时误用旧资源。

### 维护

- `AGENTS.md` 新增公开 GitHub 仓库维护规范，约束 README、CHANGELOG、`.github/`、安全文档、内部文档归档和敏感文件忽略习惯。

### 构建

- `versionName = 1.13.12`
- `versionCode = 260`
- 数据库版本保持 `25`，不新增数据迁移。

## v1.13.11

发布日期：2026-05-22

### 改进

- 今日看板 Widget 增加左右与上下安全留白，减少卡片和标题贴近桌面小组件圆角边缘的问题。
- 倒数日 Widget 增加外层留白与条目内部间距，让倒数时间、颜色竖线和事项标题更有呼吸感。

### 构建与发布

- `versionName = 1.13.11`
- `versionCode = 259`
- 数据库版本保持 `25`，不新增数据迁移。
- Debug 和 release APK 元数据均确认 `1.13.11 / 259`。
- Release APK 签名校验通过 APK Signature Scheme v2。
- GitHub Release：<https://github.com/HoshiTakyobu/PaykiTodo/releases/tag/v1.13.11>

## v1.13.10

发布日期：2026-05-22

### 改进

- 基于 `1.13.9` 工作线重新递增安装版本号，确保 Android 能覆盖安装到已经安装旧调试包的设备。

### 构建

- `versionName = 1.13.10`
- `versionCode = 258`
- 数据库版本保持 `25`。

## v1.13.9

发布日期：2026-05-22

### 修复

- 修正 Widget 待办和日程色条的一体化裁剪，减少色条与卡片边缘的拼接裂缝。
- 统一 Widget 日程聚合卡与待办卡背景风格。
- 深色模式新增更清晰的逾期 badge 背景。
- 日历事件长按拖拽期间禁用 `HorizontalPager` 用户翻页，降低拖拽日程时误触横向翻页的风险。

### 构建

- `versionName = 1.13.9`
- `versionCode = 257`
- 数据库版本保持 `25`。

## v1.13.8

发布日期：2026-05-22

### 改进

- 今日看板和倒数日 Widget 移除应用壁纸背景层，改用固定浅色 / 深色小组件背景。
- 今日看板 Widget 待办卡去掉不可交互的空心勾选圆圈。
- 逾期待办标签从 `!` 改为 `已逾期`。
- Widget 加载状态改为自定义 `加载中...` 行，避免系统默认 loading 造成误解。
- 手机日历日视图 / 三日视图改由 `HorizontalPager` 驱动横向翻页。
- 待办列表增加稳定 key 和 `contentType`，减少大量列表滚动时的复用成本。

### 构建

- `versionName = 1.13.8`
- `versionCode = 256`
- 数据库版本保持 `25`。

## v1.13.6

发布日期：2026-05-22

### 新增与改进

- 规划台 Outliner 支持手机端同级长按拖拽排序。
- 规划台新增节点级撤销栈。
- 待办页折叠未来循环实例，今天和已错过实例仍保持可见。
- 提醒页改为更高对比、更大字号的强提醒布局。
- 新增待办“闹钟模式”，重要 DDL 可持续响铃 / 振动，未处理后降级为通知并有限重试。
- 新增每日摘要通知、全局搜索、数据健康检查与安全清理。
- 日程进行中通知按开始和结束时间恢复排程。

### 数据

- 数据库升级到 `25`。
- `todo_items` 和 `recurring_task_templates` 新增 `alarmMode` 字段。

### 构建

- `versionName = 1.13.6`
- `versionCode = 254`

## v1.12.19

### 修复

- 加固循环提醒调度，避免调度失败拖垮应用启动。
- 编辑 BottomSheet 禁止半屏收缩，减少误操作和显示异常。
- 倒数日 / 日程点击优先进入详情预览。
- 新增“仅提醒，不在看板/日历显示”待办类型。
- 规划台 Outliner 新增备注节点，备注节点不发布为正式待办或日程。

### 数据

- 数据库升级到 `24`。
- 新增 `todo_items.hiddenFromBoard`、`recurring_task_templates.hiddenFromBoard` 和 `planning_nodes.isNote`。

## v1.12.15

### 改进

- 规划台新增草稿 / 发布分离：新建、分享、拍照、语音和图片识别先进入草稿节点，用户确认后再发布为正式待办或日程。
- 电脑端 Web 规划台支持草稿状态、单条发布和批量发布。
- 日历顶部月份标题和操作按钮合并到更紧凑的标题区。
- 待办分组新建入口前置。

### 数据

- 数据库升级到 `23`。
- `planning_nodes` 新增 `isDraft` 字段。

## v1.12.10

### 改进

- 手机端规划台 Outliner 改为更接近备忘录的连续输入体验。
- 规划台新增编辑 / 预览切换。
- 有子项的规划节点自动降级为结构标题，叶子节点才同步为正式事项。
- 自然日程解析支持 `时间, 事件名, 地点` 的裸地点写法。

### 构建

- `versionName = 1.12.10`
- `versionCode = 237`

## v1.11.0

### 关键变化

- 收口签名 release 构建流程。
- Release 构建启用 R8 minification 和资源收缩，显著降低 APK 体积。
- 补齐 release 签名模板与本地签名材料忽略规则。

## v1.10.x

### 关键变化

- 新增倒数日模式：待办和日程可标记为倒数目标。
- 新增独立倒数日 Widget。
- 电脑端规划台 AI 预览补齐地点、全天、倒数日、重复规则等字段。
- 修复日程地点 `@` 被重复补前缀的问题：`@` 只是用户文本的一部分。
- Android 今日看板 Widget 移除顶部标题栏，独立倒数日 Widget 改为可滚动列表。

## v1.9.x

### 关键变化

- 电脑端 Web 首页推进为每日看板。
- Android 今日看板 Widget 多轮视觉迭代，逐步贴近应用内每日看板。
- AI 日报 / 周报迁移到独立 `AI 报告` 归档入口。
- 电脑同步 API 改为按需分页和可见范围查询，减少全量快照加载。
- 无 DDL 活动待办统一归入今日待办。
- 手机端主状态和小组件刷新路径做性能收口。

## v1.8.x

### 关键变化

- 规划台成为“持续跟踪的上游规划文档”：导入行与正式待办 / 日程建立稳定映射。
- 支持导入项刷新、批量顺延、撤销和冲突处理。
- 设置页新增多 AI 源配置，支持 OpenAI-compatible `/models` 拉取模型列表。
- 公告迁移到规划台多公告行，并显示在每日看板、电脑端和 Widget。

## v1.7.x

### 关键变化

- 建立规划台早期 Markdown / 自然语言解析链路。
- 支持 `- [ ]` 待办、自然 DDL、轻量日程、混合提醒时间和 `#imported` 回写。
- 修复多轮解析器边界问题：行内日期、时间段位置、斜杠日期、中文 AM/PM、显式 `#ddl` 优先级等。
- 手机端和电脑端规划台补齐自动保存、预览、导入和使用说明。

## 版本记录规则

- 面向用户的功能、行为、数据结构和发布信息写入本文件。
- 单纯内部调试、会话交接和未发布实验细节写入 `docs/current/` 或 Git commit，不在本文件堆积。
- 如果数据库版本变化，必须在对应版本明确写出迁移字段和兼容说明。
- 如果 release 已发布，记录 GitHub Release 链接和 APK 元数据。
